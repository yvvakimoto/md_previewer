; EXAMPLE ONLY - not read by the installer.
;
; To point the installer at an internal mirror instead of the upstream npm
; registry, copy this file to `tikz-source.ini` (it is installed to
; {app}\assets\, so drop it into assets\ the same way an overlay drops in
; update.json) and set a real Url. Absent tikz-source.ini => the installer
; fetches from https://registry.npmjs.org/ as usual.
;
; Url    - either an http(s) URL or a UNC / local path to the .tgz.
; Sha256 - optional. Leave empty to skip verification; set it to pin the file.
;          The upstream digest for @rod2ik/tikzjax 1.6.0 is the one below.
;
; A one-off override is also available without this file:
;   MdPreviewer-Setup-x.y.z.exe /TIKZSRC="\\SERVER\share\rod2ik-tikzjax-1.6.0.tgz"
;
; !! LICENSING !!
; @rod2ik/tikzjax is GPL-3.0-or-later and bundles LPPL-licensed TeX packages.
; The installer deliberately does NOT carry it, so that this product never
; conveys those binaries. Putting the .tgz on a mirror makes YOU the
; distributor: within one company, handing it to your own employees for
; internal use is not "conveying" under GPLv3 and needs nothing further, but
; if the mirror is reachable from outside the organisation you must also
; accompany it with the Corresponding Source (including the sources the
; tex.wasm / core.dump binaries were built from) or a written offer for it.
; Do not put it on a publicly reachable share.

[TikZ]
Url=\\FILESERVER\share\MdPreviewer\rod2ik-tikzjax-1.6.0.tgz
Sha256=ca7d979a89136910d7f149810dd83b07d68c87fe9000fbb4ed86e28b5d780eed

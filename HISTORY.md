# 更新履歴

このファイルは MD Previewer のバージョンごとの主な更新内容をまとめたものです（新しい順）。

## v0.44.0

- **同梱している表示エンジンを、まとめて最新版に更新しました** — Markdown パーサー・作図・数式・グラフなどのライブラリを入れ替え、うち 5 つはメジャーバージョンをまたぐ更新です（marked 11 → 18、Mermaid 10 → 12、Plotly 2 → 4、KaTeX 0.16 → 0.18、js-yaml 4 → 5）。ほかに highlight.js・abcjs・TikZ エンジン・three.js・Marp コア・CodeMirror も更新しています。上流のバグ修正と改善がまとめて入りますが、**Markdown の書き方（記法）は何も変わりません**。
  - **Mermaid の図は、線の引き回しや大きさがわずかに変わります** — 配色と図の雰囲気は従来どおりになるよう固定してありますが、レイアウトの計算そのものが新しくなったため、同じ図でも幅や間隔が以前とまったく同じにはなりません。作り直していない文書を開いて図の位置が少し動いていても、異常ではありません。
  - **Mermaid の図ごとに、レイアウトと画風を選べるようになりました** — 図の先頭に次のように書くと、その図だけ別のレイアウトエンジン（`layout: elk`）や手描き風（`look: handDrawn`）にできます。何も書かなければ従来どおりの見た目です。

    ```
    ---
    config:
      layout: elk
    ---
    graph LR
      A --> B
    ```

  - **コードブロックの色分けが細かく改善されています** — highlight.js が新しくなり、Python の `self` や t-string、Rust の新しい数値リテラルなど、これまで色が付かなかった箇所が拾われるようになりました。
  - **数式で使えるコマンドが増えました** — KaTeX が新しくなり、`\reflectbox` や `\mapsfrom` などが使えます。`align` 環境まわりの表示の不具合も直っています。
  - **グラフ・楽譜・3D モデル・可換図式の描画は従来どおり**です — 折れ線・散布図・棒・ヒストグラム・箱ひげ・ヒートマップ・3D サーフェスの全種類、ABC 楽譜、model3d、tikz / tikzcd がこれまでどおり描画されることを確認しています。
- **社内ミラーから TikZ エンジンを配るときの設定例が、そのままでは使えない状態でした** — `assets/tikz-source.example.ini` と `installer/README.md` に載せていた UNC パスの例が `\SERVER\share\…` と先頭の `\` を 1 つ落としており、**書き写すと解決できないパス**になっていました。`\\SERVER\share\…` に修正し、併せて例に書かれていたバージョンと SHA-256 を現在の TikZ エンジン（1.6.0）のものに更新しています。設定ファイル自体の読み取り方は変わっていないので、すでに動いているミラー設定はそのままで問題ありません。

> 開発者向け: 同梱ライブラリの版はリポジトリ内の **7 箇所**に重複しており、CLAUDE.md が「五箇所」と書いていたのは誤りだった。漏れていた 2 箇所はどちらも**既にドリフトしていた** —— `assets/index.html` の `EXPORT_*_CDN`（HTML エクスポートに焼き込まれる CDN URL。highlight.js が実取得版 11.9.0 に対し 11.10.0 と書かれていた）と、`collect-licenses.ps1` の `$direct` 配列のハードコード版（KaTeX が `0.16.x`、Mermaid が `(bundled)` と実質ピン無し。これは利用者に配られる `THIRD_PARTY_LICENSES.txt` の元データなので、ズレは誤った法的表記になる）。両者を含む 7 箇所の整合は新設の **`pincheck.py`**（stdlib のみ・オフライン・1 秒未満）で機械検査するようにし、ミラー表の該当行を ❌ から ✅ に移した。`build.ps1` のセンチネルが abcjs 音源の `C4.mp3` を見ていたのも修正（完了マーカーは最後に取得される `C8.mp3`。途中で失敗したダウンロードを導入済みと誤判定していた）。**版だけでなく URL も確認が要る** —— marked は v16 で `marked.min.js` を廃止し（`lib/marked.umd.js`）、js-yaml は v5 で browser ビルドを `dist/browser/` に移した。どちらも旧 URL は 404 し、`fetch-libs.ps1` が throw する。どちらも `Dest` は旧ファイル名のまま置いたので `index.html` と `$libsSentinels` は無変更。KaTeX 0.18 は内部 CSS クラス 20 個を `katex-` 接頭辞付きに改名しており、`hakuro-modern.css` の `.tag` が該当した。⚠ **marp-core 4.4.0 は KaTeX 0.17 系をバンドルしている**ので、通常プレビュー（0.18）と Marp とで同じ数式が**異なるクラス名**を出す。現状どのテーマも内部クラスを触っていないので実害は無いが、`.katex` 配下に新しく CSS を書くときは両方の綴りに当てる必要がある（marp-core 5 で解消予定）。Mermaid 12 は ELK レイアウトと `neo` 配色を既定化したため `mermaidBaseConfig()` で `layout: 'dagre'` / `look: 'classic'` を明示して従来の見た目に寄せてある（図ごとの front-matter による opt-in が効くことは実測で確認）。検証は更新前ベースラインとの比較で harness 22 項目すべて PASS、`docs/` のギャラリー画像 9 点を再生成、リリースビルドと `--export-png` で実機確認。

## v0.43.0

- **可換図式（tikz-cd）・TikZ 図の日本語ラベルに、色指定が効くようになりました** — これまで日本語のラベルだけは**何色を指定してもテーマの文字色のまま**でした。同じラベルの中の欧文だけが赤くなり、`\textcolor{red}{赤 red}` が**半分だけ赤い**という状態になっていましたが、これからは**日本語も欧文とまったく同じ色**になります。サンプル: [samples/tikzcd.md](samples/tikzcd.md)。
  - **色の指定方法は問いません** — `\node[red] {…}`、`\node[text=blue] {…}`、`\draw[green!60!black] … node {…}`、`\begin{scope}[orange] … \end{scope}`、`\textcolor{…}{…}`、`\color{blue!50!black}`、`\definecolor` で自分で作った色、いずれでも効きます。
  - **ラベルの途中で色を変えても、文字ごとに追従します** — `\node[red] {黒 \textcolor{blue}{青} 赤}` のような書き方がそのまま通ります。
  - **矢印ラベルや数式の中、回転させたスコープの中でも同じ**です。**HTML エクスポート・PDF 出力・図の 1 枚保存**にもそのまま乗ります。
  - **色を指定していないラベルは、これまでどおりテーマの文字色**です（ダークモードでは明るい色に追従します）。
  - **CMYK 系の色だけは例外**です — `cyan` `magenta` `yellow` や `\color[cmyk]{…}`、dvipsnames の色名（`Maroon` など）は、同梱の TeX エンジン側の制約でテーマの文字色になります。これは**欧文もまったく同じ**なので、ラベルの片側だけ色が付くことはありません。色を確実に出したいときは `red` `blue` `green!60!black` や `\definecolor{名前}{HTML}{1B9E77}` のような RGB 系の指定を使ってください。
- **`tikz` フェンスでも `\mathbb` などの AMS 数式コマンドが使えるようになりました** — `amsmath` と `amssymb` が **`tikz` / `tikzcd` どちらのフェンスでも既定で読み込まれる**ようになり、`\mathbb{R}` `\mathfrak{g}` `\varnothing` といった AMS 記号や `\text{…}` `\dfrac` が使えます。これまで `\mathbb{R}` は `tikzcd` フェンスでは通るのに `tikz` フェンスでは**赤枠のコンパイルエラー**になり、しかもメッセージが「TeX 構文を確認してください」だったため、自分の書き方を疑って原因を探す羽目になっていました（同じブロックに日本語ラベルがあると、さらに日本語のせいだと誤って示唆されていました）。既存の図の見た目は変わりません。

> 開発者向け: 日本語ラベルの色は **TeX からは取得できない**。実測で、`\current@color` が追従するのは `\color` / `\textcolor` だけで、`\node[red]` / `text=` / パスオプション / `\begin{scope}[orange]` / `\definecolor` / tikzcd の色付き矢印ラベル / 数式ラベルは**すべて `gray 0` のまま**だった（pgf が color.sty を通らず自前で `color push` special を書くため）。xcolor の `.`（`\colorlet{x}{.}`）は未着色の図で `Undefined color` のハードエラーになり、`err.tikzJaCompile`「日本語ラベルを確認してください」でこちらのバグを作者に転嫁するので、さらに悪い。そこで TeX に訊くのをやめ **エンジンに訊く**: `run-tex.js` の `putRule()` はすべての rule を `<rect … fill="<現在の色>">` として**サイズ判定なしで**書き出すので、`\mdp@box` のストラットを `width\z@` → **`width1sp`** にして `\special` の**後ろ**へ移し、色マーカーにした（⚠ TeX は空 rule を出力しない —— `fin_rule` が `rule_wd > 0` を要求するため `\z@` では何も出ない。1sp ≒ 1.5e-5pt なので不可視、箱は固定幅のまま、連続 special のマージ回避にもう一枚効く）。新設の `__tikzAdoptJpFill()` がその `fill` を `<text>` へ写してマーカーを削除する。**tikzjax の正規化後に読む**のが要点で、マーカー矩形は `He()` の `t()` 述語（text を子に持つ親の rect）に拾われるため、黒・白 → `currentColor` の畳み込みを隣の `<text>` とまったく同じように通っている —— 写すのは最終色で、dvips の色指定を自前で解釈しないので `_()` の JS ミラーが不要になり、cmyk のフォールバックも欧文と自動一致する。`assets/libs/**` は git-ignored で `fetch-libs.ps1` が毎回消して再展開するため、`putSVG()` に `{?color}` を足す（技術的には 1 行で最もきれい）案は採れない。`{?x}` / `{?y}` は不変で、`samples/tikzcd.md` のピクセル差はアンチエイリアス 1 階調のみ。AMS 側は `__TIKZ_BASE_PACKAGES` を両フェンス共通にしただけで、`tikz-cd` は `cd` に追加する —— **フェンス種別で再分割してはいけない**。回帰ネットは新設の **`tikzcheck.py`**: tikz を見られる唯一の検証（`domdump.py` は `<svg>` を 1 ノードに畳み、`shoot.py` は `.tikz-error` を「解決済み」と数える）で、色の 19 ケースを「期待値」と「同ラベル欧文グリフとの computed fill 一致」の両方で検査し、マーカー矩形の消費・`font-size` の単位なし・HTML エクスポートへの追従・ダークモード再描画（= `__tikzCache` 復元経路）・`samples/tikzcd.md` のエラーがちょうど 1 件であることも押さえる。

## v0.42.0

- **エディタで編集している場所が、プレビュー側でも淡く色付くようになりました** — エディタ（`E` キー）でカーソルを動かすと、**いま書いている段落に対応するプレビュー側のブロックが薄く強調**されます。長い文書ほど「画面のどこを直しているのか」を目で追うのが難しくなりますが、これで両方の窓が同じ場所を指していることが一目で分かります。**既定では OFF** です。
  - **切り替えは 2 通り** — エディタの `⚙ 設定` モーダル →「**連携**」の「**カーソル行をプレビューで強調**」、または Vim の ex コマンド **`:set cursorblock`** / **`:set nocursorblock`**（`cblock` / `nocblock` でも可）。設定は保存され、次回以降も引き継がれます。
  - **強調されるのは「行」ではなく「ブロック」です** — 段落・見出し・表・図・リスト項目の単位で色が付きます。3 行にわたる段落なら、そのどこにカーソルがあっても段落全体が色付きます。
  - **ダークモードでは色も合わせて変わります。** 文字の読みやすさを損なわない濃さにしてあります。
  - **Marp スライドモードでは無効**です。スライドは 1 枚が 1 つのまとまりとして扱われるため、強調しても画面全体が色付くだけで意味がありません。
  - **HTML / PDF / PNG に出力したものには出ません。** 編集中だけの目印なので、書き出した成果物はこれまでと同じ見た目のままです。

> 開発者向け: 新しい IPC も `CustomEvent` も足していない —— エディタは既に `updateListener` から `editor:cursor:<line>` を毎回送っており、編集時は `editor:change:` が `line` を運んでいるので、プレビュー側に消費者を足しただけ。ただし受け口は **`applyEditorScroll` ではなく新設の `applyEditorCursor`** にした（前者は戻る／進むの表示位置復元にも使われており、そこへ同居させるとエディタを開いていなくても `Alt+←` でブロックが色付いたまま残る）。`applyEditorCursor(0)` が「カーソルなし」の契約で、エディタを閉じる 3 経路すべてが `clear_editor_cursor()` 経由でこれを呼ぶ。設定キーは `uiLang` と同じ**無接頭辞の共有キー `cursorBlock`**（同一オリジン localStorage + `storage` / `focus` 同期）で、値は書き手側の慣習に合わせ `'on'` / `'off'`。ex コマンドを `cursorline` にしなかったのは、エディタ自身が `highlightActiveLine()` を動かしているため（Vim 利用者の `:set nocursorline` は「自分のカーソル行」の意味になる）。`applyEditorScroll` 内にインラインで書かれていた `[data-line]` 走査は `__blockForLine()` として共有化し、スクロール先と強調先が食い違わないようにした。再適用は **必ず `paginatePreview()` の後**の 2 箇所 —— `__splitBlockAt` の浅い `cloneNode` が `class` を運ぶため、先に付けると `bunko.css` で段落の前半と後半の両方が色付く。⚠ **`__applyCursorLineHighlight()` に `__loadingFile` ガードを足してはいけない**：再適用は 2 箇所とも `loadFileFromRust` の内側で走るので、一見自明なこの最適化がライブ編集のたびに色を消す（実アプリを駆動して初めて出た不具合で、いまは `synccheck.py` のライブ編集ケースが網）。エクスポートは HTML がクローンからのクラス除去（最初の `<style>` がそのままアーティファクトに入るため必須で、ワークスペースの複数ファイル書き出しでは他ファイルにも付いてしまう）、PDF は既存の `@media print` で無効化（行番号ガターと同じ判断 —— CSS で表現できるものに stash/restore は要らない）。回帰ネットは `synccheck.py`（4 テーマ、bunko の分割段落で 1 ブロックのみ、ライブ編集の生存、戻る／進むで色が付かないこと）、`exportcheck.py`（アーティファクトと `emulate_media("print")` の 2 面）、`prefscheck.py`（設定行・ex コマンド・`updateSettingsUI` 反映・キー名の固定）。DOM ダイジェストは 12 件とも無変化。

## v0.41.0

- **文書内のすべての図を、まとめて保存できるようになりました** — 図の上ではなく **プレビューの何も無いところを右クリック**すると「**すべての図を保存…**」が出ます。**保存先フォルダを 1 回選ぶだけ**で、その文書のすべての図がそこに書き出されます。v0.38.0 で入った 1 枚ずつの保存を 10 回繰り返す必要はもうありません。
  - **形式は図ごとに最適なものが選ばれます。** ベクタ形式を持つ図（Mermaid・ABC 楽譜・markwhen・TikZ / 可換図式・Kataskeve・ファインマン図・Plotly）は `.svg`、canvas に描いている **Kataskeve3D と 3D モデル表示は `.png`** です。**どの図も飛ばされません。**
  - **ファイル名は 1 枚ずつ保存したときとまったく同じ** `<文書名>-<種類><番号>` です。同名のファイルがあれば**上書き**するので、図を描き直したあとにもう一度実行すれば、そのフォルダがそのまま最新の状態になります。
  - **いま表示していない Marp スライドの中の図も対象**です。スライド 20 枚ぶんの図を 1 回で取り出せます。
  - 図が 1 つも無い文書では、この項目は選べない表示になります。保存が終わると「**N 個の図を保存しました**」と通知が出ます（書き出せなかった図があれば、その数も一緒に出ます）。
  - **右クリックメニューに「戻る」「進む」が加わりました** — 何も無いところの右クリックがアプリのメニューになったぶん、これまでそこに出ていたブラウザ標準の戻る／進むを**アプリ側で用意**しました。行き先が無いときはグレー表示になります（`Alt+←` / `Alt+→` とマウス側面ボタンはこれまでどおりです）。**文章を選択したうえでの右クリック、画像・動画・リンクの右クリックは、従来どおりブラウザ標準のメニュー**（コピー、画像を保存 など）です。
- **可換図式（tikz-cd）と TikZ 図に、日本語のラベルを書けるようになりました** — これまで日本語を含む ` ```tikzcd ` / ` ```tikz ` フェンスは**赤枠のコンパイルエラー**になり、しかもメッセージが「TeX 構文を確認してください」だったため原因が分かりませんでした。これからは**セルにも矢印ラベルにも日本語がそのまま書けます**。サンプル: [samples/tikzcd.md](samples/tikzcd.md)。
  - 同梱の TeX エンジンは欧文フォントしか持たないため、**TeX には文字の枠だけを確保させ、字そのものはブラウザのフォントで描いています**。回転させたラベルや拡大したスコープにもそのまま追従します。
  - **矢印ラベルの日本語は、周りの欧文と同じく一段小さい文字**で揃います（tikz-cd の仕様に合わせています）。
  - **HTML エクスポート・PDF 出力・図の 1 枚保存（SVG / PNG）**でも、そのまま日本語が出ます。

> 開発者向け: 一括保存は新 IPC `savefigures:`（全項目を 1 通で送り、ホストは `pick_folder()` を 1 回 — `exportdir:` と同型）→ `FiguresSaveDone{saved, failed}` → トースト。2 フェーズのセッション方式は採らない（ホスト側に寿命付きの状態が要り、「キャンセルは無言」の契約も壊れる）。直列化がダイアログより先に走るため、メニュー項目は `{label, delay}` を返して「書き出しています…」を出す。名前は `__figureBatchName()` を `__figureFileName()` の上に積んでバイト一致させ、番号体系の分岐を防いだ。図の解決器は `__figureFromHost()` に一本化し、`__figureAt()` は「カーソル直上の `<svg>` を優先」だけを足す薄い層になった。`display:none` のスライドは `viewBox` と canvas のビットマップで通るが、**Plotly だけ実測サイズが 0 になる**ため `_fullLayout` 経由のフォールバックを追加。ホスト側の `sanitize_figure_name()` は「直さずに拒否」で 1 個のパス構成要素に限定する（保存ダイアログで利用者が直せないのはここだけのため）。空白部の右クリックを取った代償に `__nativeMenuWins()` を新設し、リスナは `#preview` から `#preview-container` へ移した（Marp デッキのレターボックス部が誰にも届いていなかった）。戻る／進むの可否は History API では問い合わせられず `history.length` も他の履歴を数えるため、`history.state.idx` + `__histIdx` / `__histMax` で追う —— `__syncHistoryLine()` の `Object.assign` がこの `idx` を守っている。tikz の日本語は、出現した文字ぶんだけ `DeclareUnicodeCharacter` を生成して `dvisvgm:raw` special の `{?x}` / `{?y}` を DVI 位置に置換させ、空の `<text>` に 16 進コードポイントだけ載せて `__tikzFillJpText` がコンパイル後に埋める方式（special 内に非 ASCII も `&` も `#` も現れないので catcode 問題を全て回避、かつメモ化の前に埋めるのでキャッシュヒット・エクスポート・図の保存に自動で乗る）。実測で判明した罠 3 点 —— 矢印ラベルは `scriptstyle` だが `1em` が math style に追随せず日本語だけ約 43% 大きくなる（`mathchoice` + `fontdimen6` で解決）、`font-size` は単位なしで出す必要がある（SVG は `pt` を CSS 経由で 4/3 倍に解決する）、連続 special のマージ回避条件は「幅ゼロの箱を出さないこと」。回帰ネットは `keycheck.py`（一括保存で 20 件追加 —— 文書順の命名、単体保存との名前一致、canvas→PNG の実バイト、deck 表示での隠れスライド、トースト 3 種、履歴の両端。⚠ 旧「prose では出さない」アサーションは意図的に反転し、選択テキストと `<img>` の委譲を代わりの網にした）と `cargo test`（`sanitize_figure_name` / `parse_figure_batch_item`）。

## v0.40.0

- **エディタを未保存のまま閉じようとすると、保存するかどうかを尋ねるようになりました** — これまでは保存していない変更が残っていても何も聞かずに閉じ、編集内容はそのまま失われていました。これからは「**保存して閉じる**」「**保存せずに閉じる**」「**キャンセル**」を選べます（`Esc` はキャンセルです）。
  - **尋ねるのは 3 つの場面です** — エディタウィンドウの **×**（`Alt+F4` を含む）、Vim の **`:q`**、そして **プレビューウィンドウの ×（アプリの終了）**。**最後のひとつはこれまで確認どころか警告もなく、エディタの未保存の編集がアプリごと消えていました。**
  - **「保存して閉じる」** はディスクに書き込んでから閉じ、プレビューもその内容に更新します。**「保存せずに閉じる」** は書き込まずに閉じ、プレビューはディスク上の内容に戻ります（従来の挙動です）。
  - **確認なしで破棄したいときは、Vim の `:q!`** がこれまでどおり無条件で閉じます。`:wq` も従来どおり保存して閉じます。
  - ダイアログはエディタのテーマ（ライト / ダーク）と表示言語（日本語 / English）にそのまま追従します。

> 開発者向け: 判断はエディタ側の 3 ボタンモーダル 1 つに集約した（文字列は `ed.quit.*` として i18n 表が持つため、Rust 側のネイティブダイアログは採らない）。tao は `CloseRequested` で窓を壊さない —— 窓を落とすのは `close_take_dirty_path()` の `take()` だけなので、イベントループを早期 `return` するだけで拒否できる。`request_close_confirm()` がエディタ窓を前面に出して `__confirmClose(quitApp)` を呼び、答えは `editor:closeconfirm:<save|discard|cancel>:<editor|app>` → `parse_close_confirm()` → `EditorCloseDecision` で戻る。`editor:close:` は `:q!` / `:wq` 用の無条件クローズとして手を付けていない。`__confirmClose` が JS 側の `dirty` を再判定するのは、Rust のミラーが `editor:change:` で true に倒れ遷移時しか訂正されず、未編集のバッファについて尋ねてしまうため。応答待ち中にもう一度閉じる操作をすると無条件で閉じる（この間 Rust は何も壊さないので、JS が固まると窓が閉じられなくなる —— その脱出口）。`quitModal` は唯一スタックしうるモーダル（設定モーダルの上に Rust が出しうる）なので、閉じる際に `isModalOpen()` を見て `status-pinned` とフォーカス返却を抑える。回帰ネットは新設の `quitcheck.py`（38 件 —— clean 時の即答、保存が答えより先に飛ぶ順序、スコープの往復、Esc チェーン先頭、`:q` / `:q!` / `:wq`）と `cargo test`（`parse_close_confirm`）。Rust 側の拒否・窓の破棄・二度押しの脱出口はハーネスからは見えないので、実機に `WM_CLOSE` を送って確認する。

## v0.39.0

- **本文の文字サイズ・本文の幅の倍率から、上限と下限がなくなりました** — これまで文字サイズは 60%〜250%、幅は 60%〜200% で頭打ちになっていましたが、**どちらも際限なく拡大・縮小できる**ようになりました。100% / 125% / 150% … という見慣れた刻みを通り過ぎたあとも `＋` / `−` を押し続けられ、大きい方は上限なし、小さい方は 0 に近づきながら止まりません。刻みの外から内側に戻ってくると、また見慣れた刻みに吸着します。
  - **倍率（%）を直接入力できるようになりました。** `S` キーのスタイル一覧にある「本文の文字サイズ（倍率）」「本文の幅（倍率）」の読み出しが入力欄になり、`400` と打って Enter を押せば一発で 400% になります（空欄・数値でない入力・1% 未満は元の値に戻ります）。
  - **刻みの端で `＋` / `−` が押せなくなることもなくなりました。** `Ctrl` + `+` / `-` / `0` と `Ctrl` + マウスホイールの操作はこれまでどおりで、`Ctrl` + `0` はいつでも 100% に戻します。設定は保存され、次回起動時にも維持されます。
  - **上下限ではない従来の制限はそのままです** — 縦書き（`tategaki.css`）の幅は 100% が上限（それ以上広げると横スクロールの画面外に出てしまうため）、**文庫本レイアウト（`bunko.css`）は引き続き対象外**で ⚙ の「文字サイズ」「1行の文字数」が調整手段になります。**Marp スライドモードでも効きません**。
- **`hakuro-modern` テーマで、本文の幅の倍率が PDF に反映されなかったのを修正しました** — 版面を狭めて（たとえば 70%）読んでいても、**このテーマのときだけ** PDF が紙面いっぱいの幅で出ていました。画面表示のほうは正しく狭まっていたため、書き出して初めて気づく不具合でした。ほかのテーマと同じく、狭めた版面はそのまま狭く印刷されます（既定の 100% のままなら、出力はこれまでと同一です）。

> 開発者向け: 固定ラダーを「アンカー刻み」に再定義し、共有の `__scaleStep()` で両端の外側へ無限に走行できるようにした（上は `__SCALE_COARSE` = +0.25 の加算で無制限、下は `__SCALE_SHRINK` = ×0.9 の等比で 0 に漸近して到達しない、内向きはアンカー列に吸着）。検証は所属判定（`indexOf`）から `__scaleSanitize()`（有限かつ 1% 以上）に緩め、`__SCALE_MIN` は上下限ではなく 0 倍・負の倍率を弾くための正値ガードになった。読み出しは `<input type="number">` で、コミットは `change`（Enter / blur）のみ —— 打鍵ごとに再レンダリングしないため。`hakuro-modern.css` の件は、テーマ自身の `@media print` がベースラインの印刷ルールと同一詳細度かつソース順で後勝ちのため、裸の `max-width: 100%` が `--md-width-scale` を紙面で完全に破棄していたもの。ベースラインと同じ `min(100%, calc(var(--md-width-scale,1) * 100%))` の形に揃えた（宣言ごと削ると今度は画面側の 40rem が印刷に効いてしまう）。「幅倍率に参加するテーマは画面と印刷の 2 箇所を宣言する必要がある」という罠として、`export.md` / `styles-theming.md` と CLAUDE.md の mirror 行に記録済み。回帰ネットは `keycheck.py`（無限走行・吸着・入力欄・拒否で 26 件追加）と `pdfcheck.py`（「本文の幅倍率」ブロックをテーマ別に実行。`<link id="user-style">` の解析完了を待ってから印字幅を実測する —— この待ちがないとベースラインを測って無条件に通る）。

## v0.38.0

- **描画された図を右クリックして、その図だけを画像ファイルに保存できるようになりました** — プレビュー上の図を右クリックすると「**SVG として保存**」「**PNG として保存**」が出ます。文書全体を書き出さなくても、図 1 枚をそのまま資料や論文に持っていけます。保存が終わると画面下部に通知が出るだけで、保存したファイルが勝手に開くことはありません。
  - **対象は、Mermaid・ABC 楽譜・markwhen・TikZ / 可換図式・Kataskeve（平面作図）・ファインマン図・Plotly が SVG と PNG の両方**、**Kataskeve3D と 3D モデル表示は PNG のみ**です（この 2 つは canvas に描いているため SVG がありません）。
  - **保存した SVG は、単体で開いても画面どおりに見えます。** 画面で使っている配色をそのまま焼き込み、背景も敷くので、**ダークモードで保存した図を他のアプリで開いても読めます**（Marp スライド内の図には、そのスライドテーマの背景色が入ります）。TikZ の Computer Modern と数式の KaTeX はフォントごと埋め込むため、字が別のフォントに化けることもありません。
  - **PNG は画面表示の 2 倍の解像度**で保存します。PowerPoint や Word に貼っても粗くなりません。
  - **エラーが出ている図、まだ組版中の TikZ、`csv` / `tsv` や通常の表には出ません**（その場合はブラウザの通常の右クリックメニューになります）。数式を右クリックしたときの **MathML / LaTeX のコピー**はこれまでどおりです。
- **本文の幅（版面）を倍率で変えられるようになりました** — `S` キーのスタイル一覧に、v0.37.0 で入った「本文の文字サイズ」のすぐ下に **「本文の幅（倍率）」** の行が増えました。60%〜200% の範囲で、`−` / `＋` / 「既定に戻す」で操作します。設定は保存され、次回起動時にも維持されます。
  - **文字サイズと幅は別々に決められます。** 文字を大きくしても 1 行の字数を保ちたいときは幅を広げる、逆に画面は広いまま行を短くして読みやすくする、といった調整ができます。
  - **左右の余白そのものは変わりません。** 変わるのは本文が使う幅（版面）だけです。
  - **`W` キーの全幅表示とは併用できません。** 幅の倍率を操作すると全幅表示は自動的に解除されます（行にその旨が表示されます）。
  - **`hakuro-modern` にも効きます。縦書き（`tategaki.css`）は 1 行の長さに効きますが、100% が上限**です（それ以上伸ばすと横スクロールの画面外に出てしまうため）。**文庫本レイアウト（`bunko.css`）は対象外**で、版面を字数で定義している都合上、⚙ の「1行の文字数」が調整手段になります（一覧の行にもその旨が出ます）。**Marp スライドモードでは効きません**（スライドは固定寸法で組まれているため）。
  - **HTML / PDF の書き出しにも反映されます**（既定の 100% のままなら、出力はこれまでと同一です）。縦書きテーマの PDF は従来どおり用紙いっぱいに流します。

> 開発者向け: 図の保存は新 IPC `savefigure:`（data は常に base64）→ `rfd` の保存ダイアログ → `FigureSaveDone` → トースト。`setupMathContextMenu()` は `setupPreviewContextMenu()` に統合した（feynmark の `equation{}` は foreignObject 内に KaTeX を描くので `.katex` と `.feynman-block` の両方に当たり、別メニューだと 2 つ同時に開く）。メニューは `svg` / `canvas` が実在するときだけ出すので、エラーボックス・組版中の TikZ・表は判定を足さずに自動で対象外になる。TikZ フォントの data URI 埋め込みは `buildExportArtifact()` から `__inlineFontFaceCss()` / `__blobToDataUrl()` として括り出し、HTML エクスポートと共有。幅の倍率は `--md-font-scale` と対になる 2 本目の乗数 `--md-width-scale` で、テーマが自分の `max-width`（縦組みは `height`）を var() 経由にすることで opt-in する。`padding` はどちらの倍率も通さない。`__fontScaleIsFixedLayout()` は両方から使うため `__previewIsFixedLayout()` に改名。回帰ネットは `keycheck.py`（図保存 17 件 — ペイロードを復号して SVG 構造まで確認 / ラダー / padding 不変 / `W` の解除 / tategaki のクランプ / bunko の拒否）、`exportcheck.py`（`maxWidth` プローブ + `width_scale` 2 ケース）、`pdfcheck.py`（印字幅の x 方向実測）、`cargo test`（`parse_figure_payload`）。あわせて公開サイト（`docs/`）の 3D モデルと Marp スライドの図を、実機をマウスでドラッグして撮ったループアニメーション（`<picture>` + `prefers-reduced-motion`）に差し替えた。

## v0.37.0

- **3D モデル（STL・CAD メッシュ）をプレビューに貼れるようになりました** — ` ```model3d ` フェンスにファイルへのパスを書くだけで、その形状がドキュメント内に描画されます。**マウスドラッグで回転、ホイールでズーム**でき、視点は文章を編集している間も保たれます。3D プリンタ用の STL や、CAD からメッシュに書き出したデータをそのまま貼れます。サンプル: [samples/model3d.md](samples/model3d.md)。
  - **対応形式は STL（バイナリ／ASCII）・OBJ（`.mtl` 対応）・PLY・glTF・GLB・3MF** です。描画ライブラリは同梱なので**オフラインで動きます**（初回だけ読み込みます）。
  - **最短形はパス 1 行だけ**です。エッジ線・ワイヤーフレーム・床グリッド・座標軸・初期カメラ角度・色・表示サイズは、フェンス内に YAML で指定できます。
  - **モデルファイルを保存し直すとプレビューも追従します。** ダークモードにも追従し、Marp スライドの中でも使えます。
  - **HTML に書き出すと PNG 画像に変換されます**（回転はできなくなりますが、1 ファイルで自己完結し、外部への通信も発生しません）。PDF と PNG 出力はそのまま印刷／撮影されます。
  - **STEP / IGES などのソリッド CAD 形式は対象外**です。CAD 側でメッシュ（STL / OBJ / glTF）に書き出してから貼ってください。Draco / KTX2 で圧縮した glTF と、glTF のアニメーション再生にも未対応です。モデルは 64 MB までです。
  - WebGL2 が使えない環境では、その場に説明つきのエラーが表示され、ドキュメントの他の部分は通常どおり表示されます。
- **本文の文字サイズを変えられるようになりました（PDF にも反映されます）** — **`Ctrl` + `+` / `-` / `0`**、または **`Ctrl` + マウスホイール**で、本文の文字を 60%〜250% の範囲で拡大・縮小できます。`S` キーのスタイル一覧の下端にも **「本文の文字サイズ（倍率）」** の行があり、`−` / `＋` / 「既定に戻す」で同じ値を操作できます。設定は保存され、次回起動時にも維持されます。
  - **これまでの `Ctrl` + `+` は画面表示だけのズームで、PDF には一切反映されませんでした。** 今回からは**レイアウト上の文字サイズそのもの**が変わるため、読みやすい大きさに合わせてから `X` で書き出せば、**PDF や HTML の字も同じ大きさで出ます**。
  - **版面（本文の幅）は変わりません。** 大きくした分だけ 1 行に入る字数が減ります（ページ全体が拡大するブラウザのズームとは別物です）。
  - **拡縮するのは本文だけです。** サイドバー・モーダルなどの画面まわり、図（Mermaid・Plotly・TikZ・3D モデルなど）、`|WxH` でサイズを指定した画像は、これまでどおりの大きさのままです。
  - **Marp スライドモードでは効きません**（スライドは固定寸法で組まれているため）。deck モードの `Ctrl` + ホイールによるスライド拡大は従来どおりです。**文庫本レイアウト（`bunko.css`）も対象外**で、版面を「39字 × 16行」という字数で定義している都合上、⚙ の「文字サイズ」「1行の文字数」が調整手段になります（一覧の行にもその旨が出ます）。
  - タッチスクリーンのピンチ操作による拡大はできなくなりました（タッチパッドのピンチ操作は引き続き使えます）。
- **PDF に通知（トースト）が写り込むことがあったのを修正しました** — スタイルを切り替えた直後などに画面下部へ出る通知が、まだ表示されている間に `X` から PDF を書き出すと、そのまま紙面に印刷されてしまうことがありました。

> 開発者向け: 文字サイズは `--md-font-scale` という CSS 変数の乗数で、`bunko.css` が PDF 用に使っている `--md-print-scale` と同型。テーマは自分の `font-size` をこの変数経由にすることで参加する（`max-width` には掛けない）。PDF / `--export-png` は live DOM を印刷するので自動的に追従し、HTML エクスポートだけ `#font-scale-style` を `svCss` と同じ形で明示的に持ち出している。WebView2 のブラウザズーム（`with_hotkeys_zoom`）は OFF にし、旧版で保存されたズーム率を消すため起動時に一度 `webview.zoom(1.0)` を呼ぶ。Marp の除外は `body:not(.marp)` セレクタによる宣言的なもので、解決タイミングの競合が原理的に起きない。`bunko.css` を不参加にしたのは、`__verticalPrepareForPdf` が計算する `--md-print-scale` が倍率の逆数に一致し紙面で完全に打ち消し合うため。回帰ネットは `pdfcheck.py`（印刷後の本文スパンを pt で実測: 12pt → 18pt、トースト非印刷）・`exportcheck.py`（非既定倍率の横組み／縦組み 2 ケース）・`keycheck.py`（全打鍵・永続化・モーダル行・版面幅の不変）。

## v0.36.0

- **タスクリストのチェックをプレビュー上で付け外しできるようになりました** — `- [ ]` / `- [x]` で書いたチェックボックスを**クリックするだけで、その 1 行が `.md` に書き戻されます**。進捗を追うためのチェックリストを、エディタを開かずそのまま使えます。もう一度クリックすれば元に戻ります。サンプル: [samples/tasklist.md](samples/tasklist.md)。
  - **入れ子や順序付きリスト（`1.`）、引用（`>`）の中、項目の間に空行があるリストでも同じように使えます。** Marp スライドの中でも使えます。
  - **書き換わるのはその 1 行だけです。** インデント、マーカーの種類（`-` / `*` / `+` / `1.`）、空白の入れ方、ファイルの改行コード（CRLF / LF）はそのまま保たれ、ほかの行には一切触れません。
  - **エディタウィンドウ（`E`）を開いているときは、その内容も同じ状態に追従します**（未保存の変更があれば、その変更ごと保存されます）。
  - **HTML / PDF / PNG に書き出したものは操作できません**（表示だけ）。見た目もこれまでと変わりません。
  - コードブロックの中に書いた `- [ ]` は文字列のままで、対象になりません。ソースと画面の対応が確かめられない場合（4 スペース字下げのコードブロックに `- [ ]` が混ざっているなど）や、脚注の本文の中にある場合は、書き戻さずにメッセージを表示します。
- **Claude Code 用の文書作成スキルが、インストーラーで導入した環境でもスライドの仕上がりを自分で確認できるようになりました** — v0.35.0 で追加したスキル（`md-previewer-author`）は、Marp スライドを書いたあと PNG を書き出して版面のはみ出しを直す手順を持っていますが、`md-previewer.exe` をコマンド名で呼ぶ前提だったため、**インストーラーで入れた環境では実行ファイルを見つけられませんでした**（インストーラーは exe を PATH に登録しません）。既定のインストール先（`%LOCALAPPDATA%\Programs\MdPreviewer\`）まで探してフルパスで起動するようになり、それでも見つからない場合は PNG での確認を省いて構造チェックだけで進めます。

> 開発者向け: チェックボックスの書き戻しは既存の汎用 `savefile:` IPC を流用し、Rust 側は無変更。位置特定は `[data-line]` 祖先で範囲を切り、その塊のソース行と DOM のチェックボックスを個数・順序・状態で突き合わせる refuse-first 方式（`__taskLocate`）で、1 行だけを `tblReplaceLines()` でオフセット splice するため EOL は不変。通常パイプラインと Marp を 1 実装で賄っている。有効化に伴って顕在化した 2 件も修正: `__isTypingTarget()` が INPUT を無条件に文字入力扱いしており、チェックボックスにフォーカスが乗ると 1 文字ショートカットが全滅していた点と、`applyTaskLists()` の marker 判定が `\s+` で改行にマッチし、`- [x]` 単体行の扱いが Marp と通常パイプラインで食い違っていた点（marked の規則に合わせた）。回帰ネットは新規の `tools/preview-harness/taskcheck.py` と `keycheck.py` のフォーカスケース。

## v0.35.0

- **ABC 楽譜を再生できるようになりました** — ` ```abc ` で書いた楽譜に**マウスを載せると、譜面の下に再生バーが現れます**。再生・一時停止・停止、**クリックで頭出しできる進行バー**、テンポ表示が並びます。**鳴っている音符が譜面上で色付く**ので、どこを演奏しているか目で追えます。再生中はマウスを外してもバーは出たままです。サンプル: [samples/abcjs.md](samples/abcjs.md)。
  - **再生用の音源（約 2MB）はインストール時に取得します** — セットアップの「**ABC 楽譜の再生用音源をダウンロード**」（要インターネット接続）で、新規インストールでは**既定でオン**です。
  - ⚠ **自動更新で上げた場合、音源は入りません。** セットアップは前回選んだ項目を引き継ぐため、今回追加されたこの項目は選ばれないままになります。再生を使いたい場合は[最新のインストーラー](https://github.com/yvvakimoto/md_previewer/releases/latest)を一度実行し、「ABC 楽譜の再生用音源をダウンロード」がオンになっているか確認してください（上書きインストールで、関連付けや既に入れた TikZ コンポーネントはそのまま残ります）。
  - **音源が無くても楽譜はこれまでどおり表示されます。** 再生ボタンだけが無効になり、バーにその理由が出ます。
  - **音源を入れたあとは、再生を含めて表示はすべてオフラインで動きます**（ネットに出るのは、この音源の取得時と、v0.34.0 で入った更新確認のときだけです）。
  - **音色はピアノのみです。** `%%MIDI program` で他の楽器を指定しても反映されません。
  - 文書を編集したり、テーマや表示言語を切り替えると、**プレビューを描き直すため再生は止まります**。HTML / PDF の書き出しに再生バーは含まれません（楽譜はこれまでどおり含まれます）。
- **Claude Code 用の文書作成スキルをインストーラーから導入できるようになりました（既定オフ）** — [Claude Code](https://claude.com/claude-code) に「この Markdown ビューア向けにスライドを作って」「技術文書を書いて」と頼めるようにするスキル（`md-previewer-author`）です。セットアップで「**Claude Code 用の文書作成スキルを導入**」をオンにすると、`%USERPROFILE%\.claude\skills\md-previewer-author\` に置かれます（アプリ本体のフォルダではありません）。**必要な方だけオンにしてください。**
  - スキル本体・記法リファレンス・本アプリの `samples/` 一式が入り、Marp スライドや `:::` の段組み、mermaid、KaTeX、縦書きといった**本アプリの拡張記法を、標準 Markdown で足りる場合と使い分けて**書いてくれます。Marp スライドでは PNG を自分で書き出し、版面のはみ出しを直してから提出します。
  - ユーザー個人のスキル置き場に入るので、**どのプロジェクトで作業していても有効**です。MD Previewer をアンインストールすると一緒に削除されます。こちらも自動更新では導入されないので、使いたい場合はインストーラーを実行してください。詳しくは README の「オプション：Claude Code 用 文書作成スキル」をご覧ください。

## v0.34.0

- **新しいバージョンが出たら自動でお知らせするようになりました** — これまでは配布元が設定ファイルを用意した場合だけ働く機能でしたが、**既定で有効**になり、更新元は **GitHub の公開リリース**です。新版があると**画面右下に小さなカード**が出て、**「今すぐ更新」**（アプリ終了 → 管理者権限なしで静かに更新 → 自動再起動。開いていた文書も開き直します）か、**「終了後に更新」**（予約だけして読み書きを続け、自分でアプリを閉じたときに更新）を選べます。「×」で今回だけ閉じる、「今後表示しない」でその端末では以後出さない、も選べます。
  - **文書の表示にネットは要りません。** 図・数式・スライドの描画に必要なものはこれまでどおりすべて同梱されており、ネットに出るのは更新確認のときだけです。確認は**最初の描画が終わってから**裏で走るので、起動やプレビュー表示を待たされることはありません。ネットに繋がらない環境では黙って何もしません。確認は**6 時間に 1 回まで**です。
  - **止めたいとき** — カードの「今後表示しない」で通知が出なくなります。機能ごと止めるには、インストール先の `assets\update.json` を削除するか `"enabled": false` に書き換えてください。以後どこにも接続しません。
  - **社内ネットワークだけで配りたいとき** — 従来どおりの**社内ファイル共有からの更新**も引き続き使えます（`update.json` の `provider` を `"share"` に）。この設定では外部インターネットへは一切アクセスしません。すでに配布済みの設定ファイルは**書き換えなしでそのまま**動きます。詳しくは README の「自動更新」をご覧ください。

> 開発者向け: CLAUDE.md を索引と横断不変条件に絞り、詳細を `.claude/docs/` の各ファイルへ移しました。

## v0.33.0

- **Marp のテーマが 8 色になり、スライドの色を自由に選べるようになりました** — 付属テーマに `magenta` / `indigo` / `purple` / `green` / `gold` / `silver` / `black` / `dark` の 8 色が揃いました。フロントマターに `theme: gold` のように書けば切り替わります。
  - さらに `S` キーのテーマ一覧に **⚙** が付き、**テーマの色そのものを好きな色に変えられます**。見出し・リンク・インラインコード・`::: message` の枠・レーザーポインタ・蛍光ペン・透かしが、選んだ色にまとめて追随します。**明るさは設計値のまま固定**なので、淡い色を選んでも濃い色を選んでも文字の読みやすさは変わりません。
  - スライドごとに `<!-- _class: invert -->` と書くと、**同じ色の暗いスライド**になります。表紙や章の区切りにお使いください。
  - 選んだ色はテーマごとに保存され、`X` キーの HTML 書き出しにもそのまま反映されます。サンプル: [samples/marp-themes.md](samples/marp-themes.md)。
- **スライドのコードブロックが読めなかった不具合を修正しました** — 濃い色のテーマでは、コードブロックが**濃い地色の内側に白い箱**として浮き、その上に薄い色の文字が乗るため、**括弧や記号がほとんど見えない**状態でした。スライドのコードブロックは**通常のプレビューと同じ配色**になり、明るいテーマでは明るく、暗いテーマでは暗く表示されます。
  - あわせて、組み込みテーマ（`default` / `gaia` / `uncover`）をお使いの場合の**リンク・表の見出し・引用・区切り線の色**も、Marp 本来の配色で表示されるようになりました（これまではアプリ側の配色が優先されていました）。通常のプレビューの見た目は変わりません。
- **セルモードで `dG` が後ろのスライドまで消してしまう不具合を修正しました** — 内蔵エディタで Vim キーバインドとセルモードを併用しているとき、`gg` / `G` が**文書の先頭・末尾**まで飛んでいたため、`dG` を押すと**カーソル行から後ろのセル（スライド）が全部消えて**しまいました。セルモードが ON のあいだ、`gg` / `G` は**カーソルのあるセルの中で止まります**。`dG` / `yG` / `cG` やビジュアルモードの `vG` / `VG` も同様です。
  - 回数を付けた `5G` は**これまでどおり絶対行**（5 行目）へ飛びます。行番号の表示と意味を合わせるためです。
  - 文書全体の先頭・末尾へは `Ctrl` + `Home` / `End` で移動できます。セルモードが OFF のときの動作は変わりません。
- **紹介ページにキーボードショートカット一覧を掲載しました** — <https://yvvakimoto.github.io/md_previewer/#keys> — プレビューのキー操作（`H` キーで出る一覧と同じ内容）と、内蔵エディタの主要なキー操作を、**ウィンドウの表示 / 開く・書き出す / Marp スライド / エディタウィンドウ**の 4 つに分けて掲載しました。日本語 / English を切り替えられます。
  - あわせて、`H` キーのヘルプ一覧に**デッキ表示の `Space`（次のスライド）**を追記しました。以前から使えていましたが、一覧に載っていませんでした。

> 開発者向け: Marp テーマは `--theme-color` 1 つから oklch の相対色で全トークンを派生する方式に変更し、7 つの派生テーマは `tools/marp-theme-gen/gen_themes.py` が hex 表から生成する。コードブロックの不具合は詳細度の競合（`<head>` で無条件に読み込む highlight.js の GitHub ライトテーマがテーマ側の `section pre code` に勝っていた）によるもので、`index.html` の要素配色を `body:not(.marp)` に限定して解消した。`gg` / `G` は upstream が両者を同一モーションに割り当てているため、`Vim.defineMotion` 1 回の差し替えでオペレータ保留形まで波及する。紹介ページのショートカット一覧は `tools/preview-harness/docskeycheck.py` がアプリのヘルプ表と機械照合する（stdlib のみ、Playwright 不要）。

## v0.32.1

- **起動時に画面が止まり、文書がなかなか表示されないことがある不具合を修正しました** — 起動のたびに行う更新確認が、**文書を表示するより先に**社内ファイル共有へアクセスしていました。共有が混んでいたり応答しなかったりすると、その待ち時間のあいだ文書の表示そのものが遅れてしまいます（共有が応答しない場合で約 2 秒の遅れを確認しました）。更新確認は**文書が画面に出てから**行うようになり、共有の状態にかかわらず、まず文書が表示されます。社内配布パッケージ向けの機能です。
- **更新のお知らせが出るまでの待ち時間をなくしました** — これまでは新しいバージョンを見つけたあと、**インストーラー（数十 MB）のダウンロードが終わるまで**更新バナーが出ませんでした。新しいバージョンを見つけた時点ですぐバナーが出るようになり、ダウンロードはその裏で進みます。ダウンロードの途中で「今すぐ更新」を押しても、そのまま共有フォルダから更新できます。

> 開発者向け: 更新確認の起点をプレビューの初回描画完了通知に変更し、描画に至らない場合に備えて 5 秒のウォッチドッグを併用しています（先着した方だけが一度だけ実行）。確認処理はマニフェスト照会とインストーラー先読みの 2 フェーズに分割し、照会直後に通知するようにしました。先読みは `.part` へ書いてから `rename` で確定します。あわせて `md-previewer.log` の各行に起動からの経過時間 `[+Nms]` を付与し、この種の「起動が遅い」報告を計測できるようにしています。

## v0.32.0

- **図形をコードで書けるようになりました（`kataskeve` / `kataskeve3d`）** — コードブロックの言語に `kataskeve` と書くと、中身を**平面幾何の作図**として図に変換します。点・線分・直線・円・多角形に加えて、重心や垂線の足のような**派生点**、平行移動・回転などの**変換**、辺の等長マークや角マークも書けます。方眼と座標軸を出すこともできます。
  - `kataskeve3d` は**立体図形**版で、球・円柱・円錐・トーラス・多面体・曲面を、**手描きのペン画のような線画**で描きます。見えない稜線は消す／薄く描く／破線にするかを選べ、点描による陰影も付けられます。視点（俯角と方位）は自由に指定できます。
  - どちらも**完全オフライン**で動き、ダーク表示に合わせて色が変わります。`X` キーの HTML 書き出しにもそのまま乗ります（立体図形は画像として埋め込まれるので、書き出したファイル 1 つで完結します）。
  - Marp スライドの中でも使えます。サンプル: [samples/kataskeve.md](samples/kataskeve.md) / [samples/kataskeve3d.md](samples/kataskeve3d.md)。
- **ファインマン図を書けるようになりました（`feynman`）** — コードブロックの言語に `feynman` と書くと、素粒子物理の**ファインマン図**を描きます。粒子線の種類（フェルミオン・光子・グルーオン・スカラー・ゴースト・重力子など）や運動量ラベル、線の曲げ、頂点の点・ブロブ・×印、ハドロンの括弧を宣言的に指定できます。**線の配置は自動**なので、座標を自分で決める必要はありません。
  - 数式の中に図をそのまま埋め込むこともできます（`equation` ブロックの中で `@図の名前` と書くと、その位置に図が入ります）。ラベルは数式として組まれます。
  - こちらも完全オフラインで、ダーク表示・HTML 書き出し・Marp スライドに対応します。サンプル: [samples/feynman.md](samples/feynman.md)。
- **図が描けなかったときに「読み込み中…」から進まなくなる不具合を修正しました** — 図の描画部品の読み込みに失敗すると、**文書そのものが表示されないまま**ぐるぐる回り続け、画面にも何の手がかりも出ませんでした。失敗しても**文書は必ず表示され**、読み込めなかった図の場所だけにエラーの枠が出るようになりました。ほかの図や本文は影響を受けません。
- **バッククォートを含むインラインコードの直後から表示が崩れる不具合を修正しました** — `` ` `` で囲んだコードの**中身にバッククォートの連続が含まれる**場合（コードブロックの書き方そのものを説明している文書など）、そこから後ろのインラインコードの対応が 1 つずつずれ、`MDFENCE11Z` のような内部用の文字列が本文に出てしまうことがありました。
- **紹介ページの実例ギャラリーを更新しました** — <https://yvvakimoto.github.io/md_previewer/> に上記 3 つの記法の実例を追加し、スライドの図版も 2 カラム + 3D グラフのものに差し替えました。

## v0.31.0

- **表示言語を日本語 / English で切り替えられるようになりました** — メニューやボタンなど、アプリの表示に使われる文字をまとめて英語にできます。**プレビューと内蔵エディタの両方**が同時に切り替わり、再起動は要りません。
  - 切り替えは、プレビューでは **`H` キーのヘルプ画面の下部**、内蔵エディタでは **`⚙ 設定`** から。**「自動 / 日本語 / English」** の 3 択です。
  - **「自動」** は Windows の言語設定に合わせます。**すでにお使いの方はこれまでどおり日本語のまま**なので、意図せず英語に変わることはありません。新しく入れた場合だけ自動判定が働きます。
  - 選んだ言語は次回以降も引き継がれ、**2 つのウィンドウで別々になることはありません**。
  - 表示言語を変えても、**開いている文書の中身は一切変わりません**。ファイル名やテーマ名もそのままです。
  - `X` キーで書き出した HTML も、書き出したときの表示言語になります（目次サイドバーの見出しなど）。
  - テーマの **⚙ 設定項目のラベル**も日英に対応しました。自作テーマでも `label` の隣に `labelEn` を書けば英語表示にできます。
- **紹介ページを公開しました** — <https://yvvakimoto.github.io/md_previewer/> — どんなことができるツールなのかを 1 ページにまとめました。記法の書き方と実際の表示を並べた実例集もあるので、**人に紹介するときや、書き方を思い出したいときの入口**としてお使いください。日本語 / English を切り替えられます。
- **ヘルプ画面（`H` キー）が読みやすくなりました** — キーの欄と説明の欄の幅のバランスを調整し、説明が細長く折り返されないようにしました。とくに英語表示のときに効きます。

> 開発者向け: リリース作業に `-Publish`（GitHub Release の作成とインストーラー添付）を追加。あわせて、`core.autocrlf` により同梱リリースノートの照合が常に誤検知していた不具合を修正した。

## v0.30.0

- **更新のお知らせに「終了後に更新」が増えました** — 社内共有からの自動更新を使っている場合、画面右下のお知らせカードのボタンが **「今すぐ更新」** と **「終了後に更新」** の 2 つになりました。
  - **「終了後に更新」** を選ぶとカードが閉じて、そのまま作業を続けられます。更新は**自分でアプリを閉じたとき**におこなわれ、次に開いたときから新しいバージョンになります（更新後にアプリが勝手に開き直すことはありません）。読みかけ・書きかけを中断せずに済みます。
  - **「今すぐ更新」** はこれまでどおりです。その場でアプリを閉じて更新し、開いていた文書ごと自動で開き直します。
  - 「×」（そのセッションだけ非表示）と「今後表示しない」（その端末で恒久的に非表示）も従来どおりです。
- **TikZ・可換図式コンポーネントを、インストール時のダウンロードに変更しました** — ` ```tikzcd ` / ` ```tikz ` の図を描く TeX エンジンは、これまでインストーラーに同梱していましたが、インストーラー本体から外し、**インストール時に配布元から取得する**方式にしました。ウィザードの **「TikZ・可換図式コンポーネントをダウンロード (約6MB, 要インターネット接続)」** で選べます（**既定でチェック済み**）。インストーラーがその分軽くなります。
  - **取得しなくても、ほかの機能はすべてこれまでどおり使えます。** チェックを外した場合や取得に失敗した場合は、`tikz` / `tikzcd` のブロックだけが「TikZ コンポーネントが未インストールです」という表示になります。あとから使いたくなったら、**インストーラーをもう一度実行してチェックを入れる**だけで有効になります（すでに入っている場合は再ダウンロードしません）。
  - 図の描画そのものは従来どおり**完全オフライン**です。インターネット接続が必要なのはインストールのときだけです。
- **スタートメニューから `使い方 (README)` を開けるようになりました** — 機能の一覧と使い方をまとめた README をインストーラーに同梱し、スタートメニューの **`使い方 (README)`** からプレビュア自身で開けます。文中の `samples/…` へのリンクもそのままクリックして、実例の文書を開けます。

> 開発者向け: 表示検証ハーネス (`tools/preview-harness`) の描画完了判定に tikz を追加し、コンパイル中のスピナーを完成と誤認しないようにした。

## v0.29.0

- **Dvorak 配列エミュレータを使っていても、Vim のコマンドが QWERTY の位置で打てるようになりました** — 内蔵エディタ（`E` キー）の設定です。やまぶきなどの配列エミュレータで Dvorak を使っていると、Vim のコマンドが「出力された文字」で解釈されるため、`hjkl` をはじめとする QWERTY 由来の指の記憶がそのまま使えませんでした。`⚙ 設定` モーダルの **「キー配列（Vim コマンド）」を `Dvorak → QWERTY`** にすると、**コマンドモード（ノーマル / ビジュアル / オペレータ待機）のキーだけ**が、押した**物理キーの QWERTY 位置**で解釈されるようになります。
  - `hjkl` の移動はもちろん、`dd`・`dw`・`gg`・`3j`・`[[` のような**複数キーの組み合わせ、オペレータと移動の組み合わせ、回数指定**もすべて QWERTY の位置どおりに効きます。`:` は `Shift` +（QWERTY で `;` のあるキー）、`/` は QWERTY で `/` のあるキーです。
  - **文章の入力は Dvorak のまま**です。インサートモードでの文字入力、`:` のコマンドライン と `/` の検索語に打つ文字は変換しないので、文章は普段どおりタッチタイプできます。`f` / `t` / `r` で「この文字まで移動」「この文字に置換」と指定した文字も、打ったとおりに扱われます。
  - Vim の ex コマンド `:set dvorak` / `:set nodvorak` / `:keylayout [qwerty|dvorak]` でも切り替えられます。設定は次回以降も引き継がれます。
  - **既定は OFF（変換なし）** なので、QWERTY をお使いの方の操作はこれまでと一切変わりません。セルモードのコマンドモードにも同じ設定が効きます。

## v0.28.0

- **`.md` リンクを別のウィンドウで開けるようになりました** — 文書中の `.md` / `.markdown` リンクを **`Ctrl`（`Cmd`）を押しながらクリック**すると、そのウィンドウで移動する代わりに**新しいプレビュアのウィンドウ**で開きます。参照元を残したまま 2 つの文書を並べて読み比べられます。
  - 押さずにクリックしたときは従来どおり同じウィンドウで移動します（`Alt+←` / `Alt+→` の履歴もそのまま）。元のウィンドウは表示位置も履歴も一切変わりません。
  - 新しいウィンドウは元のウィンドウから**少し右下にずらして**開きます。続けて `Ctrl`+クリックするたびにさらにずれていくので重なりません（ずらすと画面に収まらない場合は、Windows の「ウィンドウを重ねて表示」と同じように画面の左上へ折り返します）。
  - 開いたウィンドウは通常どおり起動したプレビュアなので、`E` キーのエディタや `X` キーの書き出しもそのまま使えます。テーマとダーク表示も引き継ぎます。
  - **Marp スライド内のリンクでも同じように使えます。**
- **Modelica のシンタックスハイライトに対応しました** — コードブロックの言語に `modelica`（別名 `mo` / `mos`）を指定すると色付けされます。Modelica 言語仕様 3.6 の予約語・組込み型・組込み関数に沿っており、複数行にまたがるドキュメント文字列や `annotation`、`'単一引用符で囲む識別子'`、`operator record` や `end when;` のような紛らわしい並びも正しく扱います。サンプル [samples/syntax.md](samples/syntax.md) に例があります。
- **縦書きテーマでエディタとプレビューの位置が連携しない不具合を修正しました** — `tategaki.css` / `bunko.css` を選んでいるとき、次の 2 つが働いていませんでした。
  - **エディタで文章を書いてもプレビューがその場所へ動かない** — 縦書きでは行が右から左へ進むため、読み始めは画面の**右端**で、スクロールも横方向です。そこを見ずに縦方向だけを見ていたため、何も起こらない状態になっていました。`E` キーでエディタを開くときにプレビューの表示位置がカーソルに反映されないのも同じ原因です。
  - **カーソルを 1 行ずつ動かすとプレビューが前後に揺れる** — 段落の間の空行などでいったん逆方向へ戻り、次の行で進み直す動きになっていました。

## v0.27.0

- **ルビ（振り仮名）を書けるようになりました** — 漢字に読み仮名を添えられます。でんでんマークダウン互換の 3 通りの書き方があり、どれも同じ結果になります。

    | 書き方 | 例 |
    |---|---|
    | バー形（確実） | `｜猫《ねこ》` |
    | 省略形（`｜` を省く） | `漢字《かんじ》` |
    | 波括弧形（半角のみで打てる） | `{吾輩\|わがはい}` |

  - 省略形は `《` の直前の**漢字の並び**が自動的に親文字になるので、送り仮名のある語でも区切りを気にせず書けます（`お天気《てんき》` の親文字は「天気」）。
  - 読みを `|` で区切り、**その数が親文字の字数とちょうど一致すると 1 字ずつに読みが乗る「モノルビ」**になります（`{漢字|かん|じ}`）。数が合わないときは語全体に 1 つ乗る形に戻ります。
  - **縦書きテーマ（`tategaki.css` / `bunko.css`）では、縦組みの作法どおり行の右側に出ます。** `bunko.css` の文庫本レイアウトでは、ルビが行の高さを押し広げて 1 ページの行数が狂わないように調整済みです（ルビの多い文章では ⚙ から行送りを少し広げると実際の文庫本に近づきます）。
  - Marp スライド・HTML 書き出し・PDF 出力にもそのまま反映されます。
  - 記号そのものを書きたいときは `\｜` `\《` `\》` とエスケープします（漢字の直後の `《》` を書名の記号として使う場合に必要です）。表のセルでは `|` が列の区切りとして先に解釈されるため、波括弧形は使えません（バー形か省略形をお使いください）。
  - 書き方と制約の一覧はサンプル [samples/ruby.md](samples/ruby.md) にまとめています。
- **Marp で自動縮小したスライドにスクロールバーが残る不具合を修正しました** — 本文が少しだけはみ出したスライドで、自動縮小（`A` キー）が働いて 1 枚に収まったあとも、右端に不要なスクロールバーが出たままになっていました（サンプルの「定義リスト」スライドなど）。本文の高さの測り方が実際の表示と 16px ずれていたためです。縮小の下限（0.5）を超えてはみ出すスライドと、`A` キーを OFF にしたときにスクロールで続きを読める挙動は従来どおりです。

## v0.26.0

- **文庫本レイアウトにノンブル（ページ番号）が付きました** — `bunko.css` を選ぶと、各ページの**地（下）の中央**にページ番号が入ります。`S` キー一覧の **⚙** から表示・非表示を切り替えられます。
- **縦書きテーマの PDF 出力が実用になりました** — `tategaki.css` / `bunko.css` で `X` キーから PDF を選ぶと、**A4 横**の用紙に、本文が右から左へ何ページにも分けて印刷されます。
  - `bunko.css` は **1 枚に 2 ページ分（見開き。1 ページ目が右）**。ページの間には見開きのノドにあたる広めのアキが入ります。図版などで幅の広くなったページは自動的に 1 枚 1 ページに落ちるので切れません。
  - 用紙の四方に **20mm の余白**が入ります。`bunko.css` では版面がそこに収まるよう自動的に縮小されますが、1 行の字数も 1 ページの行数も変わりません（相似に縮むだけです）。⚙ で字数を大きくしていても切れません。
  - `tategaki.css` は **1 行の長さも 1 ページの行数も紙のサイズから自動的に**決まります。
  - これまでは A4 縦の紙に押し込まれ、**1 ページ目に入らなかった本文が出力されずに消えていました**（短編 1 本で全体の 4 割ほどしか出ないこともありました）。
  - しおり・リンク・テキスト選択はこれまでどおり使えます。
  - 縦書きでない通常の文書、および Marp スライドの PDF 出力は**従来どおり**です。
- **PDF の本文終了後が灰色になる不具合を修正しました** — 最終ページの、本文が終わったあとの余白がアプリの背景色（薄い灰色）で塗られていました。縦書きテーマでは版面が紙の途中で終わるため、ページの大半が灰色になっていました。縦書きに限らず、すべての文書に影響していた不具合です。
- **`bunko.css` の HTML 書き出しが横組みになる不具合を修正しました** — `X` キーで HTML に書き出すと、縦組みが失われて横組みで表示されていました。`bunko.css` は `tategaki.css` を読み込む形で作られており、書き出したファイル側からその読み込みが解決できなかったためです。読み込み先の内容もまとめて書き出すようにしたので、縦組み・ページ分割・ノンブルがそのまま残ります。
- **更新通知の出方を控えめにしました** — 新しいバージョンが見つかったときのお知らせが、画面上部を横いっぱいに塗る青い帯から**右下の小さなカード**に変わりました。本文の先頭が隠れなくなり、ボタンも「更新」と閉じる「×」の 2 つに整理しています（「今後表示しない」は小さなリンクとして残しています）。配色はヘルプ画面などと揃えてあるので、ダーク表示でも浮きません。社内配布版で自動更新を有効にしている場合のみ表示されます。
- サンプル [samples/短編小説.md](samples/短編小説.md) を大幅に加筆しました（23ページ・全10節）。各節が 3〜4 ページにまたがるので、節の切れ目とページの切れ目を見分けられます。

> 開発者向け: ページ分割テーマの各ページを `.md-page` で包むようにし、縦組み PDF の面付けと HTML 書き出しはこれを基準に動きます。また `build.ps1` が呼び出し元のカレントディレクトリに依存しなくなり（`cargo` に `--manifest-path` を渡す）、外部ディレクトリ からフルパッケージをビルドできるようになっています。バナーの z-index は 9600 へ下げ、ヘルプモーダルより下に重なります。

## v0.25.0

- **Marp スライドの PDF 出力が「1 ページ = 1 スライド」になりました** — Marp 文書で `X` キーから PDF を選ぶと、**スライドと同じ大きさの用紙に、余白なしでスライドが 1 枚ずつ**収まるようになりました。これまでは A4 縦の紙に小さく載り、四方に余白が出たうえ、ページの区切りがスライドの境目と合わずに 1 枚のスライドが 2 ページに割れることがありました。front-matter で `size: 4:3` を指定した文書は 4:3 の用紙になります。背景色や背景画像も紙の端まで印刷されるので、そのまま配布資料や投影用として使えます。
  - **どの表示モードから出力しても同じ結果**になります。以前はデッキ表示（`P` キー）のまま出力すると**表示中の 1 枚しか PDF に入らず**、一覧表示のままだとサムネイルが並んだ紙が出力されていました。
  - 文字は選択・検索でき、リンクもクリックできる点はこれまでどおりです。
  - 通常の（Marp ではない）Markdown 文書の PDF 出力は**従来どおり** A4 縦・余白ありで、見出しのしおりも付きます。
  - オートフィット（`A` キー）の下限まで縮めても入りきらないスライドは、画面表示や PNG 出力と同じように、はみ出した部分が切れます（次のページには送られません）。

> 開発者向け: 用紙サイズは JS 側がスライドの実寸（`<foreignObject>` の `width`/`height`）を読み、`pdfprintready:` の応答で Rust に渡す方式です。印刷レイアウトは `@media print` だけでは作れません（`fitMarpSlides()` は画面の実測値でインライン transform を書くため、print CSS でだけ寸法が変わると測定値と印刷結果がずれます）。そこで `--export-png` の `__prepareCapture()` と同じく、印刷前に各スライドを実寸へ直して再フィットしています。改ページは隣接セレクタの `break-before`（全スライドへの `break-after` だと末尾に空白ページが出ます）。検証は `tools/preview-harness/pdfcheck.py`（Playwright + pymupdf、34 アサーション）。

## v0.24.0

- **文庫本レイアウトのテーマ `bunko.css` を追加** — `S` キーのスタイル一覧から選ぶと、縦書きの本文が **1 行 39 字 × 16 行**の文庫本の版面に組まれ、ページとページの間に余白が入ります。読み進めは縦書きらしく、右から左への横スクロールです。**段落は本物の本と同じようにページをまたいで分割**され（続きのページでは字下げしません）、見出しや図表はページをまたがず、入りきらなければ次のページの頭から始まります。見出し・引用・箇条書きの前後の余白はすべて本文 1 行分の整数倍に揃えてあるので（和文組版でいう**行取り**）、各ページはきっちり 16 行になります。ウィンドウが版面より上下に広いときは、版面を天地中央に置きます。既存の `tategaki.css` を土台にしているため、禁則処理・縦中横・約物の扱いはそのまま引き継ぎ、ダーク / ライト表示とも自由に組み合わせられます。1 行の文字数や 1 ページの行数など 9 項目は、下記の ⚙ からその場で変更できます。読み心地を試せるよう、書き下ろしの短編小説 `samples/短編小説.md`（約 7 ページ）を同梱しました。
  - 画像・コードブロック・表・数式は大きさが行の整数倍にならないため、それらを含むページだけ行数が足りなくなります（文庫レイアウトは元来、地の文のためのものです）。
- **スタイルごとの設定画面（⚙）を追加** — `S` キーのスタイル一覧で、行の右端に **⚙** が付いているスタイルは、そのスタイル専用の設定を持っています。押すとスライダーなどの設定パネルが開き、**動かすとその場でプレビューに反映**されます。設定はスタイルごとに保存され、次回起動時にも復元されます。「既定に戻す」でそのスタイルの既定値に戻ります。`bunko.css` では 1 行の文字数・1 ページの行数・文字サイズ・行送り・天地の余白・ページ間の余白・段落の字下げ・本文の書体・約物の詰めを、`tategaki.css` では文字サイズ・行送り・字間を変更できます（`tategaki.css` の既定値は従来と同一なので、これまでの見た目は変わりません）。
  - **変更できる項目は CSS の側が自分で宣言する仕組み**なので、`assets/` に置いた**自作のテーマにも同じように設定画面を持たせられます**。CSS のどこかに `/* @user-vars [ … ] */` というコメントで、変数名・ラベル・種類・範囲・既定値を書くだけです。種類はスライダー（数値・長さ）／プルダウン／色の 4 種類。範囲外の値や宣言にない値は自動的に既定値へ戻すので、あとから項目を増減しても壊れません。書き方は README の「テーマのカスタマイズ」にあります。
  - ⚙ で変えた値は、現時点では `X` キーの HTML / PDF エクスポートには反映されません（エクスポートはテーマの既定値で出力されます）。
- **縦書きの文書を開くと最後のページが表示されてしまう問題を修正** — `tategaki.css` などの縦書きテーマで文書を開くと、本文の**末尾**（画面のいちばん左）が映っており、読み始めまで自分でスクロールし直す必要がありました。開いた直後は読み始め（画面の右端）を表示します。

> 開発者向け: ページ分割は CSS では書けません（連続した本文を N 行ごとに割れるのは multicol だけですが、その段はコンテナのインライン軸＝縦書きでは下方向に並ぶため、横スクロールの縦組み本は作れません）。そこで `assets/index.html` に `paginatePreview()` を追加し、テーマが `--md-page-lines` を宣言したときだけ境目に `.md-page-break` を挿入する汎用の仕組みにしています（余白の大きさはテーマ側の CSS が決め、分割側は挿入のたびに測り直すので読みません）。段落の分割は Range の二分探索 + `extractContents()`。呼び出しは 2 か所必要で、初回ロード時は `renderMarkdown()` の完了時点で `#app-container` がまだ `display:none` のため実測できず、`loadFileFromRust()` が表示後にもう一度呼びます（分割は冪等でないので `.md-page-break` の有無で二重実行を防いでいます）。`@user-vars` はプレビュワが CSS を fetch して解析し、注入した `<style>` に `!important` 付きで適用する方式（`applyWatermarkLabel` と同じ理由）。スクロール軸の判定は `writing-mode` ではなく実測（`__previewScrollsHorizontally()`）に変更しました。

## v0.23.0

- **エディタの文字を拡大・縮小できるようになりました** — 編集ウィンドウ（`E` キー）で **`Ctrl` と `+`（`=`）／ `-` ／ `0`**、または **`Ctrl` を押しながらマウスホイール**で本文の文字サイズを変えられます（9〜32px、既定 15px、`0` で既定に戻ります）。長時間の執筆で少し大きくしたい、全体を見渡すため小さくしたい、といった場面で使えます。大きさが変わるのは**編集領域の文字と行番号だけ**で、ステータスバーや設定画面の文字は一定のままなので、拡大しても画面の枠組みは崩れません。設定はウィンドウを閉じても保持されます。Vim をお使いの場合は `:fontsize 17`（絶対指定）・`:fontsize +2` / `-2`（相対指定）・`:fontsize`（既定に戻す）も使えます。
- **エディタのフォントを選べるようになりました** — 編集領域の書体を **Cascadia Code（既定）／ Consolas ／ BIZ UDゴシック ／ 游ゴシック**から選択できます。日本語の読みやすさは文字の大きさだけでなく書体にも左右されるため、文字サイズとあわせて調整できるようにしました。
- **エディタの設定を「⚙ 設定」画面にまとめました** — これまでステータスバーに 7 個並んでいたトグルボタン（Vim・行番号・テーマ・ライブプレビュー・表の列ハイライト・セルモード）を、**`⚙ 設定` ボタン 1 つ**から開く設定画面に集約しました。文字サイズとフォントもここにあります。各項目にはキーボードショートカットや Vim の ex コマンドも併記しているので、「この機能はどのキーだったか」を思い出す場所にもなります。ステータスバーに残るのは**操作ボタンだけ**（`⚙ 設定` と `⊞ Table`、Marp 文書ではスライド操作の 3 つ）になり、以前よりすっきりしました。Vim では `:pref` でも開けます。
  - この整理にともない、**「表の貼り付け変換」の ON / OFF が初めて画面から設定できる**ようになりました。従来はボタンを増やせなかった事情から、Vim の `:set notablepaste` を知らないと切り替えられない設定でした。
  - 従来のキー操作・ex コマンド（`:set nu`・`gtc`・`gmc` など）はすべてそのまま使えます。

> 開発者向け: 文字サイズ / フォントは Compartment ではなく `:root` の CSS 変数（`--editor-font-size` / `--editor-font-family`）で実装し、`EditorView.theme` 側は `var()` で参照するだけにしてあります（状態も装飾も持たないため。`setTablePaste` と同じ規約）。変更後は `view.requestMeasure()` が必須です（`lineWrapping` 有効下で折り返し位置とキャレットがずれるため）。キーは通常の `keymap.of([...])` で足ります — `@replit/codemirror-vim` が非 INSERT で飲むのは 1 文字キーだけなので、セルモードのような `Prec.highest` ゲートは不要です。`Mod-Shift--` はセル分割が使用中のため縮小はシフト無しの `Mod--` のみとし、日本語配列向けに拡大は 3 別名を張っています。実装は `tools/build-editor/editorPrefs.js`、回帰検知は `editorPrefs.test.mjs` と `prefscheck.py`（Vim NORMAL でキーが届くか・`Ctrl+Shift+-` が今もセル分割か・ガターは追従しステータスバーは追従しないか等）。

## v0.22.0

- **tikz-cd 記法の可換図式に対応** — ` ```tikzcd ` フェンスに [tikz-cd](https://ctan.org/pkg/tikz-cd) 記法を書くと、圏論などで使う**可換図式（commutative diagram）**を図として描画します。斜め矢印・曲線矢印・二重矢印・pullback など tikz-cd の表現力をそのまま使えます。一般的な TikZ 図は ` ```tikz ` フェンス（`tikzpicture` を直接記述）で描けます。図は小さめなので**既定で 1.6 倍**に拡大し、ブロック先頭行に `scale: 2` のように書けば個別に倍率を変えられます。ダーク/ライト表示の色に追従し、通常文書・Marp スライドの両方、および HTML / PDF エクスポートにも対応します。描画はブラウザ内で完結する **完全オフライン**で、TeX エンジンは初回に一度だけ読み込まれます（`tikzcd` / `tikz` を使わない文書では読み込まれません）。サンプルは `samples/tikzcd.md`。
- **ライセンスに関する注意** — この描画には WASM 版 TeX エンジン [@rod2ik/tikzjax](https://www.npmjs.com/package/@rod2ik/tikzjax) を同梱しており、これは本アプリで初の **WebAssembly** かつ初の **コピーレフト（GPL-3.0 / LPPL-1.3c）** 依存です。アプリ本体（インストーラ）を社外配布する際は、同梱ライセンス全文（ヘルプ `H` の「Third-party licenses」に収録済み）とエンジンの無改変配布にご注意ください。なお **エクスポートした HTML / PDF には TeX エンジン自体は含まれず**、描画結果の図と（自由に再配布できる Computer Modern）フォントのみなので、成果物の配布は従来どおり自由です。

> 開発者向け: `assets/index.html` の `renderTikzCached`（メモ化 `__tikzCache`、`ensureTikz` で assetBaseUrl を絶対 URL 化した blob Web Worker 描画、`__applyTikzScale` の既定 1.6 倍）で実装。tikzjax の dist は `tools/fetch-libs.ps1` が npm ターボールを展開して `assets/libs/tikzjax/` に取得（git 管理外）。`get_mime_type` に `.wasm` を追加。HTML エクスポートは使用フォントの woff2 を base64 で `@font-face` 埋め込み。`--export-png` は非表示ウィンドウで WASM ワーカーが動かないため tikz を取得しません。

## v0.21.0

- **任意の透かし文字を指定できる `watermark` に対応** — これまで機密透かしは front-matter の `confidential: true` で「CONFIDENTIAL」の斜め透かしを出すだけでしたが、代わりに **`watermark: DRAFT`** のように書くと、任意の文字（`DRAFT` / `社外秘` / `SAMPLE` など）を同じ斜め透かしとして表示できるようになりました。透かしの色・書体・角度・大きさは従来どおり各テーマが決め、文言だけが差し替わります。`confidential: true` は従来どおり「CONFIDENTIAL」を表示し、両方を書いた場合は `watermark` の文言が優先されます。HTML / PDF エクスポートにもそのまま反映されます。
- **見出しの下線を廃止し、`---` を本文で唯一の区切り線に** — 既定（Default）テーマで、これまで見出し（`#` / `##`）の下に引いていた横罫線をなくしました。見出しの下線は章区切りに使う `---`（水平線）と見分けがつきにくかったためで、これからは見出しは文字サイズと余白の大小で階層を示し、`---` だけが本文中の横線になります。あわせて `---` の上下の余白を広げ、章の切れ目としてはっきり見えるようにしました。この変更が及ぶのは Default テーマの見た目のみで、`S` キーで選ぶ各テーマ（parchment / classical / tategaki など）は各自の見出し体裁を保ちます。Marp スライドの見出し余白も従来どおりです。
- **Office の表を貼り付けると Markdown の表に変換** — 編集ウィンドウ（`E` キー）で Excel・Word・PowerPoint などからコピーした表をそのまま貼り付けると、GFM のパイプ表（`| … | … |`）に自動変換して挿入するようになりました。

> 開発者向け: 見出し／`---` の余白まわりは、既定値を `:where()` で特定性 0 にして各テーマの指定が必ず勝つ構造にし、`<hr>` の色・余白を `--md-hr-*` カスタムプロパティで上書き可能にしました。表の貼り付けは `tablePaste.js`（回帰検知 `tablePaste.test.mjs` / `pastecheck.py`）です。

## v0.20.0

- **エディタに Jupyter 風のセルモードを追加** — エディタウィンドウ（`E` キー）のステータスバーで **`⌗ Cells: ON`** にすると、`---` 区切りが Jupyter のような「セル」として扱われ、文書を塊ごとに動かせるようになります。Marp スライドに限らずどの Markdown でも使えます（`---` はスライドの区切りと同じ単位なので、Marp では 1 セル = 1 スライドです）。`Esc` で**コマンドモード**（アクティブなセルの左端が青くなります）、`Enter` または `i` で**編集モード**（緑）に戻ります。コマンドモードでは `j` / `k` でセル間の移動、`a` / `b` で上下へのセル挿入、`dd` で削除、`x` / `c` / `v` でカット・コピー・貼り付け、`M` で下のセルと結合、`1`〜`6` で見出しレベルの変更ができます（`h` を押すとキーの一覧が出ます）。**どの操作も「元に戻す」1 回で完全に戻ります。** `Shift+Enter` でプレビューをそのセルの位置に合わせて次のセルへ進み、`Ctrl+Enter` はその場に留まり、`Alt+Enter` は下に新しいセルを作ります。**`Live: OFF` と組み合わせると、プレビューは `Shift+Enter` を押したときだけ更新される手動ワークフロー**になります。各セルは枠で囲まれ、左端に `[1]` `[2]` … のセル番号が付き、先頭の YAML 部分は灰色の保護セルとして扱われて削除や結合ができません。Vim をお使いの場合も NORMAL モードの `Esc` がコマンドモードへの入口になります（INSERT / VISUAL 中の `Esc` は従来どおりです）。既定はオフで、`⌗ Cells: OFF` に戻せば表示もキー操作も従来の編集に完全に戻ります。
- **スライドの区切り判定を修正** — 段落のすぐ次の行に `---` を書いた場合、Markdown の規則ではそれは直前の段落を見出しにする記号でスライドの区切りではありませんが、これを区切りとして数えていました。そのため **それ以降のすべてのスライドで、エディタの編集位置とプレビューの表示位置の対応がずれる** 不具合がありました（エディタのカーソルにスライドが追従しない、プレビューをクリックしても別の行へ飛ぶ、といった症状の原因です）。あわせて、`~~~` で囲んだコードブロックの中に ``` を含む場合にスライドの範囲を誤って判定し、スライドの切り取り・コピーが 1 枚ずれることがある不具合と、編集内容をプレビューへ反映した直後のスクロール位置が 1 回分古い表示をもとに計算されていた不具合も修正しました。

> 開発者向け: `---` の走査（front-matter・コードフェンス・setext 見出しの判定）をエディタ側の共有モジュール `mdBlocks.js` に集約し、プレビュー側 `renderMarp()` と同じ規則に揃えました。セル操作は純関数の planner と 3 行の dispatcher に分離してあり、回帰検知は `cells.test.mjs`（118 件。区切りの不変条件を全操作 × 全セル位置で検査）と `cellcheck.py`（Playwright でエディタを実際に動かし、キー入力が Vim より先に届いているか・1 操作 = 1 undo か・セル N = スライド N かを検証）です。あわせて `tablecheck.py` が CRLF チェックアウトの環境で失敗していた問題を修正しました。

## v0.19.0

- **表を画面上で編集できるようになりました** — プレビューに表示されている表にマウスを乗せると **「Edit table」** ボタンが出ます。押すとその表が編集モードになり、セルをクリックしてそのまま書き換えられます。行や列の挿入・削除、列の左／中央／右揃えは、**セルを右クリック**したメニューから行えます。**「Save」** で `.md` ファイルに書き戻し、**「Cancel」** で元に戻ります。編集中のセルは Markdown のもとの書き方（`**太字**`・数式・リンクなど）で表示されるため、記法が失われることはありません。保存しても、桁を揃えた書き方や改行コードといったもとの体裁はそのまま保たれ、触っていない行は 1 文字も変わりません。引用やリストの中に入れ子になった表、`csv` / `tsv` ブロックの表、Marp スライドの表は対象外です。
- **編集モードに表の作成支援を追加** — 行数・列数（と揃え）を指定して表のひな形を挿入できるようになりました。ステータスバーの **`⊞ Table`** ボタンのほか、`:table 3 4` などのコマンドや `Ctrl+Alt+T` からも呼び出せます。あわせて、カーソルがあるセルの列を表全体で薄く色づける **列ハイライト** を追加しました（**`Col: ON`** / **`Col: OFF`** で切替、既定オン）。列数の多い表でいまどの列を編集しているかが一目で分かります。
- **編集モードとプレビューのスクロール連動を修正** — コードブロックや脚注を含む文書で、エディタのカーソル位置とプレビューの表示位置が食い違う不具合を修正しました。コードブロックが多い文書ほどずれが大きくなり、文書の後半ではまったく別の場所へ飛んでしまうことがありました。プレビューをクリックしてエディタ側のカーソルを移動する操作も同様に直っています。
- **数式の `\left` 自動ペア挿入を修正** — 自動で挿入された `\right…` が Vim の削除操作（`d$` や `x` など）で消しても復活してしまい、取り除けなくなる不具合を修正しました。あわせて、離れた位置にすでに `\right` があるときの二重挿入を防ぎ、カーソルがペアのちょうど間にあるときは **Backspace 1 回で `\left…` `\right…` の両方**を削除できるようにしました。

> 開発者向け: 表の書き戻しは `data-line`（描画結果とソース行の対応）に依存するため、行数を保存していなかった 2 つの前処理（コードフェンスのマスクと脚注定義の除去）を修正しました。これがスクロール連動のずれの原因でもあります。回帰検知として、表のモデル層の往復一致を検証する `table-model.test.cjs` / `mdTable.test.mjs` と、保存内容を実際に照合する `tablecheck.py` を追加しています。

## v0.18.1

- **自動更新のお知らせが表示されないことがある不具合を修正** — 社内ファイル共有に新しいバージョンが公開されていても、起動のタイミングによっては画面上部の更新バナーが出ず、そのまま気付けないことがありました（社内配布パッケージ向けの機能です）。あわせて、バナーを表示したか・「今後表示しない」で抑止したかがログに残るようになり、更新が届かないときの原因を確認できるようになりました。

> 開発者向け: リリース手順を二段階化し、リリースノートを人手でレビューするまでインストーラーのビルド・コミット・タグを行わないようにしました（生成されたままの下書きが同梱・自動表示されるのを防ぐため）。同梱ノートの陳腐化検査も内容ハッシュ比較に変更しています。

## v0.18.0

- **定義リストに対応** — `用語` の次の行に `: 説明` と書くと定義リスト（`<dl>`）として表示されます。1 つの用語に複数の説明、説明の中に段落・リスト・コードブロック・画像を書くこともできます。通常の文書と Marp スライドの両方で使えます。
- **ワークスペースの目次でフォルダ名が誤って表示される不具合を修正** — `_toc.md` で入れ子のリストを書いたとき、フォルダの見出しが「最後の子項目の名前」になってしまう問題を修正しました（例:「Chapters」が「Advanced」と表示されていました）。あわせて、`_toc.md` に `.md` 以外のリンク（外部 URL など）を書いた場合にファイル項目として扱われてしまう問題も修正しています。
- **Marp スライドの脚注ポップアップを修正** — Marp スライドで脚注にマウスを乗せてもポップアップが出なかった問題を修正しました。同じ脚注を複数のスライドから参照した場合は、いま見ているスライド側の本文が表示されます。
- **編集モードの補完に `2xl` を追加** — インラインスタイルの `size=` 補完に `2xl`（`xxl` と同じ大きさ）が出るようになりました。

> 開発者向け: 重複していた実装を共有ヘルパへ集約する内部リファクタリングを行いました（表示・動作の変更はありません）。あわせて回帰検知の仕組みを整備しています — `main.rs` の純粋関数のユニットテスト、`#preview` の構造を差分比較する `domdump.py`、ショートカットや右クリックメニューの挙動を検証する `keycheck.py`。

## v0.17.0

- **エクスポート後に自動で開く** — HTML / PDF をエクスポートすると、生成したファイルが既定のプログラム（ブラウザ・PDF ビューアなど）で自動的に開くようになりました。ワークスペースのフォルダ出力では `index.html` が開きます。
- **動画を含む文書の PDF 出力** — 文書内の動画を PDF 化する際、任意のタイミングのフレームを静止画として書き出せるようになりました（画像の `|t=秒数` 指定）。
- **PDF の容量削減** — PDF 出力時に高解像度画像を表示サイズに合わせて縮小し、ファイル容量を抑えるようにしました。
- **Marp スライドの表示位置を保持** — 戻る / 進むでの移動時に、表示していたスライド位置・スクロール位置を復元するようになりました。
- **Marp スライド内のリンク修正** — Marp スライド内の相対パスの `.md` リンクをクリックで開けるよう修正しました。

## v0.16.0

- **起動時の自動更新（社内配布向け・既定オフ）** — 社内ファイル共有に置いた最新版を起動時に確認し、新しければ画面上部のバナーから更新できる機能を追加しました。既定はオフで、設定ファイルが無い通常のビルドは従来どおり完全オフラインで動作します（社内配布パッケージでのみ有効化されます）。
- **Marp スライドのタスクリスト対応** — Marp スライドでも `- [ ]` / `- [x]` がチェックボックスとして表示されるようになりました。
- **インストール先フォルダを開く（`Ctrl+D`）** — 実行ファイルと `assets` があるインストール先フォルダをエクスプローラーで開くショートカットを追加しました。
- **編集モードでのフロントマター補完** — YAML front-matter のキー・値（`marp` / `theme` / `_class` など）を入力補完できるようになりました。
- **ヘルプ・UI の日本語化** — ショートカット一覧やトースト表示などのメッセージを日本語に統一しました。

## v0.15.0

- **Markwhen カレンダー表示** — `markwhen` タイムラインを、ヘッダーに `display: calendar` を書くと月間カレンダー表示に切り替えられるようになりました。オンライン時は日本の祝日も反映されます。
- **Marp スライドのレイアウト改善** — 表・Plotly 図を既定で左右中央に配置するようにし、Marp ネイティブ記法（`w:` / `h:`）での動画サイズ指定にも対応しました。
- **Mermaid 図の文字切れを修正** — ノードラベルの下部が欠けてしまう不具合を修正しました。
- **ヘッドレス PNG 出力（`--export-png`）** — Marp デッキをスライドごとに PNG 画像へ書き出せる CLI モードを追加しました（レイアウトの自動チェック向け）。通常文書を PNG 出力したとき下部に余白が付く不具合もあわせて修正しています。

> 開発者向け: ビルド不要でプレビュー UI をブラウザで確認できるプレビューハーネス（`tools/preview-harness/`）を追加しました。

## v0.14.0

- **機密表示（Confidential）** — 文書の front-matter に `confidential: true` を書くと、ページ全体に「機密（CONFIDENTIAL）」の斜め透かしが表示されます（文書ごとの指定）。見た目は各テーマ CSS が `--confidential-*` で定義でき、色・文言・大きさ・不透明度を自由に変更したり非表示にしたりできます（縦書きテーマは「社外秘」を縦組みで表示）。通常プレビュー・Marp スライドの両方に対応し、HTML / PDF エクスポートにも反映されます。
- **Marp: 本文がはみ出すスライドをスクロール可能に** — タイトル・ヘッダー・フッターを固定したまま、はみ出した本文をスクロールして最後まで読めるようになりました。
- **YAML front-matter を表示しないよう修正** — 文書先頭の front-matter（`---` で囲んだ設定ブロック）がプレビューに表示されてしまう不具合を修正しました。

## v0.13.0

- **Marp: 図の自動上下中央配置を廃止し `::: vcenter` を新設** — 文中の大きな図が上下の文章と重なる問題を解消しました。図はドキュメント順に普通に流れ（左右中央寄せは維持）、はみ出す場合は自動縮小（`A`）が本文を縮めて 1 枚に収めます。図や文を上下・左右とも中央に置きたいときは `::: vcenter … :::` で明示できます（通常文書／Marp スライド両対応）。
- **Marp 自動縮小の改善** — タイトル（H2）・ヘッダー・フッターは固定サイズのまま、本文エリアだけを縮小して 1 枚に収めるようになりました（スライド間でタイトルの大きさが揃います）。

## v0.12.0

- feat: 作成年の取得機能追加

## v0.11.0

- **PDF エクスポートに対応** — `X` キーの保存ダイアログで拡張子（`HTML` / `PDF`）を選ぶだけで出力形式を切り替えられるようになりました。PDF は**見出しから「しおり（ブックマーク）」を自動生成**し、本文中のリンクや脚注などの**リンクがクリック可能**なまま保持されます（テキスト選択可・テーマ背景保持・A4 縦）。単一ファイル表示時のみ対応（ワークスペースの `X` は従来どおり HTML フォルダ出力）。

## v0.10.1

- **日本語（非ASCII）を含むパスの画像・動画参照が表示されない不具合を修正** — `![](日本語/画像.png)` のようにフォルダ名・ファイル名に日本語を含む参照が正しく表示されるようになりました（画像・動画・`plotly` の `file:` 共通）。ファイル名にリテラルの `%` を含む場合も扱えます。

## v0.10.0

- **画像などのファイル参照で親階層 (`../`) を辿るパスに対応** — `![](../images/foo.png)` のように開いている文書より上のフォルダを参照できます（画像・動画・`plotly` の `file:` 共通）。エディタのパス補完にも親フォルダへ上がる `../` 候補を追加しました。
- **markwhen タイムラインのプレビューに対応** — ` ```markwhen ` フェンスに [markwhen](https://markwhen.com/) 記法を書くと、横型のタイムラインとして描画されます。

## v0.9.1

- fix: インストーラーのエラーおよびHISTORY.ｍての文字化けを修正

## v0.9.0

- **新規ファイル作成（Ctrl+N）** — 保存ダイアログで場所を選び、空の `.md` を作成して開く
- **`.mdx` バンドル形式に対応** — Markdown と画像/CSV/動画を1つの ZIP にまとめて開く・編集・再パック
- **Marp スライドの目次ペイン** — スクロール／一覧表示でスライド目次を表示
- **アプリのバージョン表示** — ヘルプモーダル（Hキー）にバージョンを表示
- Marp エクスポート時のレイアウト崩れを修正

## v0.8.0

- **Marpスライドの自動縮小（Aキー）** — 内容がスライドに収まらない場合、1枚に収まるよう自動で縮小（既定ON）
- **選択テキストのマーカー機能（Marp）** — スライド上で文字を選択すると蛍光ペン風のハイライトが付き、約2秒で自動的に消える
- **Pandoc風のインライン文字装飾** — `[テキスト]{color=red size=lg}` のように文字色・背景・サイズ・フォント・太さを指定（Marpでも有効）
- **多段組み `::: columns`** — 通常文書でも複数カラムレイアウトが可能に。`:::` ブロックの入力補助（オートコンプリート）も追加
- **ドキュメント作成スキルの追加** — md-previewer 向けの文書／スライド作成を支援

## v0.7.0

- **目次の「すべて閉じる／すべて開く」** — ファイルツリーの右クリックメニューから一括開閉
- **全幅表示トグル（Wキー）** — 本文を画面いっぱいに広げて表示
- **Marpレーザーポインター（Zキー）** — デッキ表示中にマウス追従の光点＋残像を表示
- フォルダを開いた際の目次インデントを調整

## v0.6.2

- **表のコピー（PowerPoint貼り付け対応）** — 表の「Copy table」ボタンで PowerPoint / Word / Excel にネイティブな表として貼り付け
- **VimモードのOSクリップボード連携** — エディタ間・他アプリ間で `y`／`p` によるコピー＆ペーストが可能に
- **KaTeX数式のコピー** — 数式の右クリックで MathML / LaTeX をコピー
- **Marpデッキのズーム** — `Ctrl+ホイール` でマウス位置を中心に拡大／ドラッグでパン
- **Mermaidの `scale:` 指定** — 図の表示倍率を指定可能に
- **強調メッセージ構文 `::: message`** — 中央寄せの強調メッセージブロック

## v0.6.1

- **ABC譜面表示** — ` ```abc ` コードブロックで楽譜（五線譜）を表示
- **動画・YouTube埋め込み** — 画像記法 `![](clip.mp4)` / `![](YouTube URL)` でローカル動画や YouTube を埋め込み
- **センタリング・右寄せ構文** — `::: center` / `::: right` / `::: left` でブロックを揃える
- **Marp脚注のページ毎表示** — 脚注を参照スライドの下部に表示
- Marp画像の読み込み速度を改善
- ドラッグ＆ドロップでエディタが開けない問題を修正、改行仕様・エディタを開く際のカーソル位置・箇条書き内の補完挙動を改善

## v0.5.0

- **Plotlyグラフ表示** — ` ```plotly ` コードブロックで外部 CSV/TSV からインタラクティブなグラフを描画
- **ライブプレビューのON/OFF切り替え** — 重い文書向けに、エディタのライブ更新を停止可能に

## v0.4.0

- **括弧の自動挿入** — エディタで `\left(` 等の対応する閉じ括弧を自動補完
- **KaTeXコマンド補完** — 数式コンテキストでの `\command` オートコンプリート
- **classical スタイル** — 格調高い書籍風テーマを追加
- **未保存マーク表示** — 編集中はウィンドウタイトルに「•」を表示
- エディタのライブプレビューが切れる問題を修正

## v0.3.0

- **日本語のVimカーソル移動を改善** — 漢字／ひらがな／カタカナ等の文字種境界で `w`／`b`／`e` 等が停止
- **Vim Escで半角入力に切替** — NORMALモード移行時に IME を自動的に半角（OFF）へ

## v0.2.0

- **スタイル切り替えの刷新** — `S`キーで一覧から選択、`M`キーはダーク／ライト切り替え専用に
- Marpプレビューと既存CSSの混在を解消
- 縦書き（tategaki）スタイルの右寄せを修正

## v0.1.0（初版）

- **リアルタイムMarkdownプレビュー** — 外部編集を自動リロード、完全オフライン動作
- **豊富な記法** — Mermaid図・KaTeX数式・シンタックスハイライト・CSV/TSVテーブル
- **画像対応** — 相対パス解決、Obsidian風のサイズ指定
- **Marpプレゼンテーション** — スクロール／デッキ／一覧の3表示モード
- **コンパニオンエディタ** — CodeMirror 6 + Vim、ライブプレビュー、オートコンプリート
- **ワークスペース（ディレクトリ）モード** — ファイルツリー、`_toc.md` 対応
- **テーマ・表示** — ダーク／ライト切替、ユーザースタイル、自動セクション番号
- **単体HTMLへのエクスポート**、ヘルプモーダル、ズームショートカット

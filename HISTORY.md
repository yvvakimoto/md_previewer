# 更新履歴

このファイルは MD Previewer のバージョンごとの主な更新内容をまとめたものです（新しい順）。

## v0.10.1

- **日本語（非ASCII）を含むパスの画像・動画参照が表示されない不具合を修正** — `![](日本語/画像.png)` のようにフォルダ名・ファイル名に日本語を含む参照が正しく表示されるようになりました（画像・動画・`plotly` の `file:` 共通）。ファイル名にリテラルの `%` を含む場合も扱えます。

## v0.10.0

- **画像などのファイル参照で親階層 (`../`) を辿るパスに対応** — `![](../images/foo.png)` のように開いている文書より上のフォルダを参照できます（画像・動画・`plotly` の `file:` 共通）。エディタのパス補完にも親フォルダへ上がる `../` 候補を追加しました。
- **markwhen タイムラインのプレビューに対応** — ` ```markwhen ` フェンスに [markwhen](https://markwhen.com/) 記法を書くと、横型のタイムラインとして描画されます。

## v0.9.1

- fix: インストーラーのエラーおよびHISTORY.ｍての文字化けを修正

## v0.9.0

- **新規ファイル作成（Ctrl+N）** — 保存ダイアログで場所を選び、空の `.md` を作成して開く
- **`.mdx` バンドル形式に対応** — Markdown と画像/CSV/動画を1つの ZIP にまとめて開く・編集・再パック
- **Marp スライドの目次ペイン** — スクロール／一覧表示でスライド目次を表示
- **アプリのバージョン表示** — ヘルプモーダル（Hキー）にバージョンを表示
- Marp エクスポート時のレイアウト崩れを修正

## v0.8.0

- **Marpスライドの自動縮小（Aキー）** — 内容がスライドに収まらない場合、1枚に収まるよう自動で縮小（既定ON）
- **選択テキストのマーカー機能（Marp）** — スライド上で文字を選択すると蛍光ペン風のハイライトが付き、約2秒で自動的に消える
- **Pandoc風のインライン文字装飾** — `[テキスト]{color=red size=lg}` のように文字色・背景・サイズ・フォント・太さを指定（Marpでも有効）
- **多段組み `::: columns`** — 通常文書でも複数カラムレイアウトが可能に。`:::` ブロックの入力補助（オートコンプリート）も追加
- **ドキュメント作成スキルの追加** — md-previewer 向けの文書／スライド作成を支援

## v0.7.0

- **目次の「すべて閉じる／すべて開く」** — ファイルツリーの右クリックメニューから一括開閉
- **全幅表示トグル（Wキー）** — 本文を画面いっぱいに広げて表示
- **Marpレーザーポインター（Zキー）** — デッキ表示中にマウス追従の光点＋残像を表示
- フォルダを開いた際の目次インデントを調整

## v0.6.2

- **表のコピー（PowerPoint貼り付け対応）** — 表の「Copy table」ボタンで PowerPoint / Word / Excel にネイティブな表として貼り付け
- **VimモードのOSクリップボード連携** — エディタ間・他アプリ間で `y`／`p` によるコピー＆ペーストが可能に
- **KaTeX数式のコピー** — 数式の右クリックで MathML / LaTeX をコピー
- **Marpデッキのズーム** — `Ctrl+ホイール` でマウス位置を中心に拡大／ドラッグでパン
- **Mermaidの `scale:` 指定** — 図の表示倍率を指定可能に
- **強調メッセージ構文 `::: message`** — 中央寄せの強調メッセージブロック

## v0.6.1

- **ABC譜面表示** — ` ```abc ` コードブロックで楽譜（五線譜）を表示
- **動画・YouTube埋め込み** — 画像記法 `![](clip.mp4)` / `![](YouTube URL)` でローカル動画や YouTube を埋め込み
- **センタリング・右寄せ構文** — `::: center` / `::: right` / `::: left` でブロックを揃える
- **Marp脚注のページ毎表示** — 脚注を参照スライドの下部に表示
- Marp画像の読み込み速度を改善
- ドラッグ＆ドロップでエディタが開けない問題を修正、改行仕様・エディタを開く際のカーソル位置・箇条書き内の補完挙動を改善

## v0.5.0

- **Plotlyグラフ表示** — ` ```plotly ` コードブロックで外部 CSV/TSV からインタラクティブなグラフを描画
- **ライブプレビューのON/OFF切り替え** — 重い文書向けに、エディタのライブ更新を停止可能に

## v0.4.0

- **括弧の自動挿入** — エディタで `\left(` 等の対応する閉じ括弧を自動補完
- **KaTeXコマンド補完** — 数式コンテキストでの `\command` オートコンプリート
- **classical スタイル** — 格調高い書籍風テーマを追加
- **未保存マーク表示** — 編集中はウィンドウタイトルに「•」を表示
- エディタのライブプレビューが切れる問題を修正

## v0.3.0

- **日本語のVimカーソル移動を改善** — 漢字／ひらがな／カタカナ等の文字種境界で `w`／`b`／`e` 等が停止
- **Vim Escで半角入力に切替** — NORMALモード移行時に IME を自動的に半角（OFF）へ

## v0.2.0

- **スタイル切り替えの刷新** — `S`キーで一覧から選択、`M`キーはダーク／ライト切り替え専用に
- Marpプレビューと既存CSSの混在を解消
- 縦書き（tategaki）スタイルの右寄せを修正

## v0.1.0（初版）

- **リアルタイムMarkdownプレビュー** — 外部編集を自動リロード、完全オフライン動作
- **豊富な記法** — Mermaid図・KaTeX数式・シンタックスハイライト・CSV/TSVテーブル
- **画像対応** — 相対パス解決、Obsidian風のサイズ指定
- **Marpプレゼンテーション** — スクロール／デッキ／一覧の3表示モード
- **コンパニオンエディタ** — CodeMirror 6 + Vim、ライブプレビュー、オートコンプリート
- **ワークスペース（ディレクトリ）モード** — ファイルツリー、`_toc.md` 対応
- **テーマ・表示** — ダーク／ライト切替、ユーザースタイル、自動セクション番号
- **単体HTMLへのエクスポート**、ヘルプモーダル、ズームショートカット
